<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="http://schemas.telerik.com/2008/xaml/compile"
    xmlns:controls="clr-namespace:Telerik.Windows.Controls;assembly=Telerik.Windows.Controls.GridView"
    xmlns:grid="clr-namespace:Telerik.Windows.Controls.GridView;assembly=Telerik.Windows.Controls.GridView"
    xmlns:input="clr-namespace:Telerik.Windows.Controls;assembly=Telerik.Windows.Controls.Input"
    xmlns:mat="clr-namespace:Telerik.Windows.Controls.MaterialControls;assembly=Telerik.Windows.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mccb="clr-namespace:Telerik.Windows.Controls.MultiColumnComboBox;assembly=Telerik.Windows.Controls.GridView"
    xmlns:searchPanel="clr-namespace:Telerik.Windows.Controls.GridView.SearchPanel;assembly=Telerik.Windows.Controls.GridView"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
    xmlns:telerik1="clr-namespace:Telerik.Windows.Controls;assembly=Telerik.Windows.Controls"
    xmlns:treeList="clr-namespace:Telerik.Windows.Controls.TreeListView;assembly=Telerik.Windows.Controls.GridView"
    mc:Ignorable="c">
    <ResourceDictionary.MergedDictionaries>
        <telerik:GreenResourceDictionary />
    </ResourceDictionary.MergedDictionaries>
    <telerik1:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    <telerik1:DoubleToThicknessConverter x:Key="DoubleToThicknessConverter" />
    <telerik1:ThicknessToOrientedThicknessConverter x:Key="ThicknessToOrientedThicknessConverter" />
    <Geometry x:Key="ArrowTopRight8x8">M0,0 L8,0 8,8 z</Geometry>
    <Geometry x:Key="ArrowBottomRight7x7">M 7,0 L 7,7 L 0,7 Z</Geometry>
    <Geometry x:Key="ArrowRight">M0,0 L4,3 L0,6z</Geometry>
    <Geometry x:Key="ArrowDown">M0,0 L6,0 L3,4z</Geometry>
    <Geometry x:Key="ArrowUp">M0,4 L6,4 L3,0z</Geometry>
    <Geometry x:Key="ThinArrowRight5x8">M5,4 L1,0 0,0 0,1 3,4 0,7 0,8 1,8z</Geometry>
    <Geometry x:Key="ArrowRight5x9">M0,0 L0,9 L5,4.5z</Geometry>
    <Geometry x:Key="CloseIcon8x8">M1,0 L4,3 L7,0 L8,1 L5,4 L8,7 L7,8 L4,5 L1,8 L0,7 L3,4 L0,1 z</Geometry>
    <Geometry x:Key="OptionMark">M0,3 L3,6 L7,0</Geometry>
    <Geometry x:Key="NoneEffect">M5,0 C3.7,0 2.4,0.4 1.4,1.4 C-0.4,3.4 -0.4,6.5 1.4,8.5 C3.4,10.4 6.5,10.4 8.5,8.5 C10.4,6.5 10.4,3.4 8.5,1.4 C7.5,0.4 6.2,0 5,0 C5,0 5,0 5,0 z M5,1.9 C5.7,1.9 6.5,2.2 7.1,2.8 C8,3.7 8.2,5.1 7.7,6.2 C7.7,6.2 3.7,2.2 3.7,2.2 C4.1,2 4.5,1.9 5,1.9 C5,1.9 5,1.9 5,1.9 z M2.3,3.5 C2.3,3.5 6.4,7.6 6.4,7.6 C5.3,8.3 3.8,8.1 2.8,7.1 C1.8,6.1 1.6,4.6 2.3,3.5 C2.3,3.5 2.3,3.5 2.3,3.5 z M2.3,3.5 L2.3,3.5 L2.3,3.5 z</Geometry>
    <Geometry x:Key="EditIndicatorIcon">M3,2 L6,2 6,3 5,3 5,7 6,7 6,8 3,8 3,7 4,7 4,3 3,3 z M2,1 L2,2 1,2 1,8 2,8 2,9 14,9 14,8 15,8 15,2 14,2 14,1 z M1,0 L15,0 15,1 16,1 16,9 15,9 15,10 1,10 1,9 0,9 0,1 1,1 z</Geometry>
    <Geometry x:Key="EditableInvertedIcon">M4,0 L12,0 12,6 4,6 4,5 3,5 3,1 4,1 z M0,0 L1,0 1,1 2,1 2,5 1,5 1,6 0,6 z</Geometry>
    <Geometry x:Key="ErrorIndicatorIcon">M4,15 L12,15 12,16 4,16 z M12,14 L13,14 13,15 12,15 z M3,14 L4,14 4,15 3,15 z M13,13 L14,13 14,14 13,14 z M2,13 L3,13 3,14 2,14 z M14,12 L15,12 15,13 14,13 z M1,12 L2,12 2,13 1,13 z M7,11 L7,13 9,13 9,11 z M15,4 L16,4 16,12 15,12 z M0,4 L1,4 1,12 0,12 z M14,3 L15,3 15,4 14,4 z M7,3 L7,10 9,10 9,3 z M1,3 L2,3 2,4 1,4 z M13,2 L14,2 14,3 13,3 z M4,2 L12,2 12,3 13,3 13,4 14,4 14,12 13,12 13,13 12,13 12,14 4,14 4,13 3,13 3,12 2,12 2,4 3,4 3,3 4,3 z M2,2 L3,2 3,3 2,3 z M12,1 L13,1 13,2 12,2 z M3,1 L4,1 4,2 3,2 z M4,0 L12,0 12,1 4,1 z</Geometry>
    <Geometry x:Key="ExclamationMarkIcon">M0,8 L2,8 2,10 0,10 z M0,0 L2,0 2,7 0,7 z</Geometry>
    <Geometry x:Key="DropImpossibleIcon">M2.7,3.7 L2.6,3.7 C2.2,4.4 2,5.1 2,6 C2,8.2 3.7,10 6,10 C6.7,10 7.5,9.7 8.1,9.3 L8.2,9.3 z M6,2 C5.1,2 4.4,2.2 3.7,2.6 L3.7,2.6 L9.2,8.3 L9.3,8.2 C9.7,7.5 10,6.8 10,6 C10,3.7 8.2,2 6,2 z M6,0 C9.3,0 12,2.6 12,6 C12,7.3 11.5,8.6 10.7,9.7 L10.3,10.1 L9.9,10.5 C8.8,11.4 7.5,12 6,12 C2.6,12 0,9.3 0,6 C0,4.6 0.4,3.3 1.2,2.2 L1.2,2.2 L1.5,2 L1.5,1.9 C2.6,0.7 4.2,0 6,0 z</Geometry>
    <Geometry x:Key="PointerRight8x8">M0,5 L0,3 L5,3 L4,2 L4,0 L8,4 L4,8 L4,6 L5,5 z</Geometry>
    <Geometry x:Key="PlusIcon5x5">M2,0 L3,0 3,2 5,2 5,3 3,3 3,5 2,5 2,3 0,3 0,2 2,2 z</Geometry>
    <Geometry x:Key="NewRowIcon">M1,1 L1,3 15,3 15,1 z M0,0 L16,0 16,4 0,4 z</Geometry>
    <Geometry x:Key="PlusIcon6x6">M2,0 L4,0 4,2 6,2 6,4 4,4 4,6 2,6 2,4 0,4 0,2 2,2 z</Geometry>
    <Geometry x:Key="ErrorIndicatorCircleIcon">M3,13 L11,13 11,14 3,14 z M11,12 L12,12 12,13 11,13 z M2,12 L3,12 3,13 2,13 z M12,11 L13,11 13,12 12,12 z M1,11 L2,11 2,12 1,12 z M13,3 L14,3 14,11 13,11 z M0,3 L1,3 1,11 0,11 z M12,2 L13,2 13,3 12,3 z M1,2 L2,2 2,3 1,3 z M11,1 L12,1 12,2 11,2 z M2,1 L3,1 3,2 2,2 z M3,0 L11,0 11,1 3,1 z</Geometry>
    <Geometry x:Key="FilterIcon">M0,0 L11,0 L7,5 L7,9 L4,9 L4,5 z</Geometry>
    <sys:String x:Key="GlyphClose"></sys:String>
    <telerik:BooleanToOpacityConverter x:Key="BooleanToOpacityConverter" />
    <telerik:UppercaseConverter x:Key="UppercaseConverter" />
    <grid:GridLineWidthToThicknessConverter x:Key="GridLineWidthToThicknessConverter" />
    <grid:FilterOperatorConverter x:Key="FilterOperatorConverter" />
    <grid:FilterCompositionLogicalOperatorConverter x:Key="FilterCompositionLogicalOperatorConverter" />
    <grid:DistinctValueConverter x:Key="DistinctValueConverter" />
    <grid:FilterOperatorToFilterEditorIsEnabledConverter x:Key="FilterOperatorToFilterEditorIsEnabledConverter" />
    <grid:SortingIndexConverter x:Key="SortingIndexConverter" />
    <grid:SortingIndexToVisibilityConverter x:Key="SortingIndexToVisibilityConverter" />
    <grid:ControlPanelItemButtonTooltipToVisibilityConverter x:Key="ControlPanelItemButtonTooltipToVisibilityConverter" />
    <grid:ColumnHeaderConverter x:Key="ColumnHeaderConverter" />
    <ControlTemplate x:Key="GridViewValidationToolTipTemplate" TargetType="ToolTip">
        <Grid
            x:Name="Root"
            Margin="2,0,0,0"
            Background="Transparent"
            Opacity="0"
            RenderTransformOrigin="0 0">
            <Grid.RenderTransform>
                <TranslateTransform x:Name="xform" X="-25" />
            </Grid.RenderTransform>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Border
                Grid.Column="1"
                MinHeight="20"
                Padding="4,3,4,3"
                Background="{telerik1:GreenResource ResourceKey=ValidationBrush}">
                <ItemsControl ItemsSource="{TemplateBinding Content}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock
                                MaxWidth="250"
                                VerticalAlignment="Center"
                                FontFamily="{telerik1:GreenResource ResourceKey=FontFamily}"
                                FontSize="{telerik1:GreenResource ResourceKey=FontSize}"
                                Foreground="{telerik1:GreenResource ResourceKey=FixedBrush}"
                                Text="{Binding}"
                                TextWrapping="Wrap" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </Border>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsOpen" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard x:Name="OpenAnimation">
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="xform"
                                Storyboard.TargetProperty="X"
                                To="0"
                                Duration="0:0:0.2" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard x:Name="CloseAnimation">
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="xform"
                                Storyboard.TargetProperty="X"
                                To="-25"
                                Duration="0:0:0.2" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
                <Setter TargetName="Root" Property="Opacity" Value="1" />
            </Trigger>
            <Trigger Property="IsOpen" Value="False">
                <Setter TargetName="Root" Property="Opacity" Value="0" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="AggregateResultsListStyle" TargetType="grid:AggregateResultsList">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <ItemsPresenter />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                        <TextBlock
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Text="{Binding Caption}" />
                        <TextBlock
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Text="{Binding FormattedValue}" />
                    </StackPanel>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource AggregateResultsListStyle}" TargetType="grid:AggregateResultsList" />
    <ControlTemplate x:Key="AutoCompleteStringFilterEditorTemplate" TargetType="grid:AutoCompleteStringFilterEditor">
        <Grid MinWidth="100">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <input:RadAutoCompleteBox
                x:Name="PART_RadAutoCompleteBox"
                VerticalAlignment="Stretch"
                AutoCompleteMode="Suggest"
                ItemsSource="{Binding RawDistinctValues}"
                SelectionMode="Single" />
            <telerik:RadToggleButton
                Grid.Column="1"
                MinWidth="20"
                Margin="5,0,0,0"
                Content="aA"
                FontFamily="{telerik1:GreenResource ResourceKey=FontFamily}"
                FontSize="{TemplateBinding FontSize}"
                IsChecked="{Binding IsCaseSensitive, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                Visibility="{TemplateBinding MatchCaseVisibility}">
                <ToolTipService.ToolTip>
                    <ToolTip telerik:LocalizationManager.ResourceKey="GridViewFilterMatchCase" />
                </ToolTipService.ToolTip>
            </telerik:RadToggleButton>
        </Grid>
    </ControlTemplate>
    <Style x:Key="AutoCompleteStringFilterEditorStyle" TargetType="grid:AutoCompleteStringFilterEditor">
        <Setter Property="Template" Value="{StaticResource AutoCompleteStringFilterEditorTemplate}" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="FontSize" Value="{telerik1:GreenResource ResourceKey=FontSize}" />
    </Style>
    <Style BasedOn="{StaticResource AutoCompleteStringFilterEditorStyle}" TargetType="grid:AutoCompleteStringFilterEditor" />
    <ControlTemplate x:Key="ControlPanelItem_DropDownButtonTemplate" TargetType="telerik:RadDropDownButton">
        <Grid>
            <Border
                x:Name="OuterBorder"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="{TemplateBinding CornerRadius}" />
            <Border
                x:Name="OuterMouseOverBorder"
                Background="{telerik1:GreenResource ResourceKey=MouseOverBrush}"
                BorderBrush="{telerik1:GreenResource ResourceKey=MouseOverBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                Visibility="Collapsed" />
            <Border
                x:Name="CheckedVisual"
                Background="{telerik1:GreenResource ResourceKey=AccentHighBrush}"
                BorderBrush="{telerik1:GreenResource ResourceKey=AccentHighBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                Visibility="Collapsed" />
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ContentPresenter
                    x:Name="Content"
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="{TemplateBinding Padding}"
                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                    Content="{TemplateBinding Content}"
                    ContentStringFormat="{TemplateBinding ContentStringFormat}"
                    ContentTemplate="{TemplateBinding ContentTemplate}"
                    ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                    RecognizesAccessKey="True"
                    TextBlock.FontFamily="{TemplateBinding FontFamily}"
                    TextBlock.FontSize="{TemplateBinding FontSize}"
                    TextBlock.Foreground="{TemplateBinding Foreground}">
                    <ContentPresenter.RenderTransform>
                        <TranslateTransform />
                    </ContentPresenter.RenderTransform>
                </ContentPresenter>
            </Grid>
            <Popup x:Name="DropDownPopup">
                <Grid>
                    <Border
                        x:Name="DropDownPopupBorder"
                        Width="{TemplateBinding DropDownWidth}"
                        Height="{TemplateBinding DropDownHeight}"
                        MinWidth="3"
                        MinHeight="3"
                        MaxWidth="{TemplateBinding DropDownMaxWidth}"
                        MaxHeight="{TemplateBinding DropDownMaxHeight}"
                        Background="{telerik1:GreenResource ResourceKey=MainBrush}"
                        BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                        BorderThickness="1"
                        Visibility="{Binding DropDownContent, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource ControlPanelItemButtonTooltipToVisibilityConverter}}">
                        <ContentControl
                            x:Name="DropDownPopupContent"
                            Margin="4"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Content="{TemplateBinding DropDownContent}"
                            ContentTemplate="{TemplateBinding DropDownContentTemplate}"
                            DataContext="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=DataContext}"
                            Foreground="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
                    </Border>
                </Grid>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsOpen" Value="True">
                <Setter TargetName="CheckedVisual" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="OuterMouseOverBorder" Property="Visibility" Value="Visible" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="ControlPanelItem_DropDownButtonStyle" TargetType="telerik:RadDropDownButton">
        <Setter Property="Template" Value="{StaticResource ControlPanelItem_DropDownButtonTemplate}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="IsOpen" Value="false" />
        <Setter Property="Margin" Value="0,0,10,0" />
        <Setter Property="Padding" Value="3" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <ControlTemplate x:Key="ControlPanelItemControlTemplate" TargetType="grid:ControlPanelItemControl">
        <telerik:RadDropDownButton
            x:Name="PART_ControlPanelItemControlDropDownButton"
            MinWidth="22"
            MinHeight="22"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            DropDownContent="{Binding Content}"
            Style="{StaticResource ControlPanelItem_DropDownButtonStyle}">
            <ToolTipService.ToolTip>
                <ToolTip
                    Content="{Binding ButtonTooltip}"
                    Placement="Right"
                    Visibility="{Binding ButtonTooltip, Converter={StaticResource ControlPanelItemButtonTooltipToVisibilityConverter}}" />
            </ToolTipService.ToolTip>
            <Grid Margin="0,3">
                <Border
                    Width="16"
                    Height="2"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Background="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
                <Border
                    Width="16"
                    Height="2"
                    Margin="0,4,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Background="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
                <Border
                    Width="16"
                    Height="2"
                    Margin="0,8,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Background="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
            </Grid>
        </telerik:RadDropDownButton>
    </ControlTemplate>
    <Style x:Key="ControlPanelItemControlStyle" TargetType="grid:ControlPanelItemControl">
        <Setter Property="Template" Value="{StaticResource ControlPanelItemControlTemplate}" />
        <Setter Property="FontSize" Value="{telerik1:GreenResource ResourceKey=FontSize}" />
        <Setter Property="FontFamily" Value="{telerik1:GreenResource ResourceKey=FontFamily}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource ControlPanelItemControlStyle}" TargetType="grid:ControlPanelItemControl" />
    <ControlTemplate x:Key="ChildDataControlsPresenterTemplate" TargetType="grid:ChildDataControlsPresenter">
        <ItemsControl
            x:Name="PART_ChildDataItemsControl"
            IsTabStop="{TemplateBinding IsTabStop}"
            ItemsSource="{Binding}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border
                        Background="{telerik1:GreenResource ResourceKey=AlternativeBrush}"
                        BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                        BorderThickness="0,1,0,1">
                        <controls:RadGridView
                            MaxHeight="300"
                            Margin="11"
                            IsTabStop="{Binding IsTabStop, ElementName=PART_ChildDataItemsControl}" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ControlTemplate>
    <Style x:Key="ChildDataControlsPresenterStyle" TargetType="grid:ChildDataControlsPresenter">
        <Setter Property="Template" Value="{StaticResource ChildDataControlsPresenterTemplate}" />
    </Style>
    <Style BasedOn="{StaticResource ChildDataControlsPresenterStyle}" TargetType="grid:ChildDataControlsPresenter" />
    <ControlTemplate x:Key="CommonColumnHeaderTemplate" TargetType="grid:CommonColumnHeader">
        <Grid x:Name="PART_HeaderCellGrid">
            <Border
                x:Name="GridViewHeaderCell"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}" />
            <ContentPresenter
                x:Name="ContentPresenter"
                Margin="{TemplateBinding Padding}"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                Content="{Binding Content, Converter={StaticResource UppercaseConverter}, RelativeSource={RelativeSource TemplatedParent}}"
                ContentTemplate="{TemplateBinding ContentTemplate}"
                TextBlock.FontFamily="{TemplateBinding FontFamily}"
                TextBlock.FontSize="{TemplateBinding FontSize}"
                TextBlock.Foreground="{TemplateBinding Foreground}" />
        </Grid>
    </ControlTemplate>
    <Style x:Key="CommonColumnHeaderStyle" TargetType="grid:CommonColumnHeader">
        <Setter Property="Template" Value="{StaticResource CommonColumnHeaderTemplate}" />
        <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="FontSize" Value="{telerik1:GreenResource ResourceKey=FontSize}" />
        <Setter Property="FontFamily" Value="{telerik1:GreenResource ResourceKey=FontFamily}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource CommonColumnHeaderStyle}" TargetType="grid:CommonColumnHeader" />
    <ControlTemplate x:Key="CommonHeaderPresenterTemplate" TargetType="grid:CommonHeaderPresenter">
        <Border
            HorizontalAlignment="Stretch"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            Visibility="{TemplateBinding Visibility}">
            <Grid x:Name="PART_CommonHeadersContainer" HorizontalAlignment="Left" />
        </Border>
    </ControlTemplate>
    <Style x:Key="CommonHeaderPresenterStyle" TargetType="grid:CommonHeaderPresenter">
        <Setter Property="Template" Value="{StaticResource CommonHeaderPresenterTemplate}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource CommonHeaderPresenterStyle}" TargetType="grid:CommonHeaderPresenter" />
    <ControlTemplate x:Key="DataCellsPresenterTemplate" TargetType="grid:DataCellsPresenter">
        <Grid>
            <ItemsPresenter />
            <grid:FrozenColumnsSplitter x:Name="PART_FrozenColumnsSplitter" HorizontalAlignment="Left" />
            <grid:FrozenColumnsSplitterRightSide x:Name="PART_FrozenColumnsSplitterRight" HorizontalAlignment="Left" />
        </Grid>
    </ControlTemplate>
    <Style x:Key="DataCellsPresenterStyle" TargetType="grid:DataCellsPresenter">
        <Setter Property="Template" Value="{StaticResource DataCellsPresenterTemplate}" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <grid:GridViewCellsPanel />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource DataCellsPresenterStyle}" TargetType="grid:DataCellsPresenter" />
    <ControlTemplate x:Key="DetailsPresenterTemplate" TargetType="grid:DetailsPresenter">
        <Border
            x:Name="DetailsBorder"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}">
            <ContentPresenter
                x:Name="PART_ContentPresenter"
                Margin="{TemplateBinding Padding}"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="Visibility" Value="Visible">
                <Setter TargetName="DetailsBorder" Property="Visibility" Value="Visible" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="DetailsPresenterStyle" TargetType="grid:DetailsPresenter">
        <Setter Property="Template" Value="{StaticResource DetailsPresenterTemplate}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Visibility" Value="Collapsed" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource DetailsPresenterStyle}" TargetType="grid:DetailsPresenter" />
    <ControlTemplate x:Key="DistinctFilterControlTemplate" TargetType="grid:FilteringDropDown">
        <Grid>
            <Button x:Name="PART_DropDownButton">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter />
                    </ControlTemplate>
                </Button.Template>
                <Grid Background="Transparent" Cursor="Hand">
                    <Path
                        Width="12"
                        Height="11"
                        Margin="2"
                        Data="{StaticResource FilterIcon}"
                        Fill="{TemplateBinding Foreground}"
                        Opacity="0.65"
                        Stretch="Fill" />
                    <Path
                        Width="12"
                        Height="11"
                        Margin="2"
                        Data="{StaticResource FilterIcon}"
                        Fill="{telerik1:GreenResource ResourceKey=AccentLowBrush}"
                        Stretch="Fill"
                        Visibility="{TemplateBinding FunnelFillVisibility}" />
                </Grid>
            </Button>
            <Popup
                x:Name="PART_DropDownPopup"
                AllowsTransparency="True"
                PopupAnimation="Slide"
                StaysOpen="True" />
        </Grid>
    </ControlTemplate>
    <Style x:Key="FilteringDropDownStyle" TargetType="grid:FilteringDropDown">
        <Setter Property="Template" Value="{StaticResource DistinctFilterControlTemplate}" />
        <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
    </Style>
    <Style BasedOn="{StaticResource FilteringDropDownStyle}" TargetType="grid:FilteringDropDown" />
    <Style x:Key="DraggedElementStyle" TargetType="controls:DraggedElement">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:DraggedElement">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <ContentPresenter
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                            <Grid
                                x:Name="PART_NoCursor"
                                Width="18"
                                Height="18"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Bottom"
                                Visibility="Collapsed">
                                <Path
                                    Width="10"
                                    Height="10"
                                    Data="{StaticResource NoneEffect}"
                                    Fill="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                                    Stretch="Fill" />
                            </Grid>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="VerticalContentAlignment" Value="Top" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource DraggedElementStyle}" TargetType="controls:DraggedElement" />
    <Style x:Key="DistinctValueListBoxItemStyle" TargetType="ListBoxItem">
        <Setter Property="Padding" Value="3" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Top" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <ContentPresenter
                        x:Name="contentPresenter"
                        Margin="{TemplateBinding Padding}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <DataTemplate x:Key="ActionTemplate">
        <TextBlock Text="{Binding Converter={StaticResource FilterOperatorConverter}}" />
    </DataTemplate>
    <DataTemplate x:Key="LogicalOperatorTemplate">
        <TextBlock Text="{Binding Converter={StaticResource FilterCompositionLogicalOperatorConverter}}" />
    </DataTemplate>
    <ControlTemplate x:Key="FilteringControlTemplate" TargetType="grid:FilteringControl">
        <Grid Background="Transparent" SnapsToDevicePixels="True">
            <Border
                Margin="{TemplateBinding Padding}"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}">
                <Grid>
                    <StackPanel
                        Margin="{TemplateBinding Padding}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                        <StackPanel x:Name="PART_DistinctFilter" Visibility="{TemplateBinding DistinctFiltersVisibility}">
                            <CheckBox
                                x:Name="PART_SelectAllCheckBox"
                                Margin="0,0,0,4"
                                telerik:LocalizationManager.ResourceKey="GridViewFilterSelectAll"
                                IsChecked="{Binding SelectAll, Mode=TwoWay}" />
                            <ListBox
                                x:Name="PART_DistinctValuesList"
                                Padding="4"
                                Background="{telerik1:GreenResource ResourceKey=AlternativeBrush}"
                                ItemContainerStyle="{StaticResource DistinctValueListBoxItemStyle}"
                                ItemsSource="{Binding DistinctValues}"
                                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                SelectionMode="Multiple">
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel MaxHeight="242" />
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox
                                            VerticalAlignment="Center"
                                            Content="{Binding ConvertedValue, Converter={StaticResource DistinctValueConverter}}"
                                            IsChecked="{Binding IsActive, Mode=TwoWay}" />
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                        <StackPanel Visibility="{TemplateBinding FieldFiltersVisibility}">
                            <TextBlock
                                Margin="0,6"
                                telerik:LocalizationManager.ResourceKey="GridViewFilterShowRowsWithValueThat"
                                Foreground="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
                            <input:RadComboBox
                                x:Name="PART_Filter1ComboBox"
                                MinHeight="22"
                                ItemTemplate="{StaticResource ActionTemplate}"
                                ItemsSource="{Binding AvailableActions}"
                                SelectedItem="{Binding Filter1.Operator, Mode=TwoWay}" />
                            <ContentControl
                                x:Name="PART_Filter1ContentControl"
                                Margin="0,4,0,0"
                                HorizontalContentAlignment="Stretch"
                                VerticalContentAlignment="Stretch"
                                DataContext="{Binding Filter1}"
                                IsEnabled="{Binding Operator, Converter={StaticResource FilterOperatorToFilterEditorIsEnabledConverter}}" />
                            <input:RadComboBox
                                x:Name="PART_LogicalOperatorsComboBox"
                                MinHeight="22"
                                Margin="0,4,0,0"
                                ItemTemplate="{StaticResource LogicalOperatorTemplate}"
                                ItemsSource="{Binding LogicalOperators}"
                                SelectedItem="{Binding FieldFilterLogicalOperator, Mode=TwoWay}" />
                            <input:RadComboBox
                                x:Name="PART_Filter2ComboBox"
                                MinHeight="22"
                                Margin="0,4,0,0"
                                ItemTemplate="{StaticResource ActionTemplate}"
                                ItemsSource="{Binding AvailableActions}"
                                SelectedItem="{Binding Filter2.Operator, Mode=TwoWay}" />
                            <ContentControl
                                x:Name="PART_Filter2ContentControl"
                                Margin="0,4,0,0"
                                HorizontalContentAlignment="Stretch"
                                VerticalContentAlignment="Stretch"
                                DataContext="{Binding Filter2}"
                                IsEnabled="{Binding Operator, Converter={StaticResource FilterOperatorToFilterEditorIsEnabledConverter}}" />
                        </StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <Button
                                x:Name="PART_ApplyFilterButton"
                                MinWidth="89"
                                Margin="0,4,0,0"
                                telerik:LocalizationManager.ResourceKey="GridViewFilter"
                                Visibility="{TemplateBinding FilterButtonVisibility}" />
                            <Button
                                x:Name="PART_ClearFilterButton"
                                Grid.Column="1"
                                MinWidth="89"
                                Margin="4,4,0,0"
                                telerik:LocalizationManager.ResourceKey="GridViewClearFilter" />
                        </Grid>
                    </StackPanel>
                    <telerik:RadButton
                        x:Name="PART_FilterCloseButton"
                        Grid.Column="1"
                        Width="12"
                        Height="12"
                        MinHeight="0"
                        Margin="0,9,9,0"
                        Padding="0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        BorderThickness="0">
                        <Grid UseLayoutRounding="True">
                            <Path
                                Width="8"
                                Height="8"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Data="{StaticResource CloseIcon8x8}"
                                Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                                SnapsToDevicePixels="True"
                                Stretch="Fill" />
                            <Path
                                Width="8"
                                Height="8"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Data="{StaticResource CloseIcon8x8}"
                                Fill="{telerik1:GreenResource ResourceKey=SelectedBrush}"
                                SnapsToDevicePixels="True"
                                Stretch="Fill"
                                Visibility="{Binding IsPressed, ElementName=PART_FilterCloseButton, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        </Grid>
                    </telerik:RadButton>
                </Grid>
            </Border>
        </Grid>
    </ControlTemplate>
    <Style x:Key="FilteringControlStyle" TargetType="grid:FilteringControl">
        <Setter Property="Template" Value="{StaticResource FilteringControlTemplate}" />
        <Setter Property="MinWidth" Value="210" />
        <Setter Property="Padding" Value="10" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=AlternativeBrush}" />
        <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="FontWeight" Value="Normal" />
    </Style>
    <Style BasedOn="{StaticResource FilteringControlStyle}" TargetType="grid:FilteringControl" />
    <DataTemplate x:Key="FilterOperatorItemTemplate">
        <TextBlock Text="{Binding Converter={StaticResource FilterOperatorConverter}}" />
    </DataTemplate>
    <Style x:Key="FieldFilterDropDownButtonStyle" TargetType="telerik:RadDropDownButton">
        <Setter Property="IsOpen" Value="false" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="telerik:RadDropDownButton">
                    <Grid>
                        <ContentPresenter
                            x:Name="Content"
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}" />
                        <Popup x:Name="DropDownPopup">
                            <ContentPresenter
                                x:Name="DropDownPopupContent"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                Content="{TemplateBinding DropDownContent}"
                                ContentTemplate="{TemplateBinding DropDownContentTemplate}"
                                DataContext="{TemplateBinding DataContext}" />
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <ControlTemplate x:Key="FieldFilterControlTemplate" TargetType="grid:FieldFilterControl">
        <Border
            Padding="0,2,0,1"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}">
            <Grid x:Name="PART_MainGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ContentControl
                    x:Name="PART_FilterEditorContentControl"
                    Margin="3,0,3,1"
                    HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                    IsEnabled="{Binding EditorIsEnabled}" />
                <telerik:RadDropDownButton
                    x:Name="PART_DropDownButton"
                    Grid.Column="1"
                    DropDownIndicatorVisibility="Collapsed"
                    KeepOpen="False"
                    Style="{StaticResource FieldFilterDropDownButtonStyle}">
                    <Border
                        Padding="2,0,5,0"
                        Background="Transparent"
                        Cursor="Hand">
                        <Grid
                            Width="12"
                            Height="11"
                            Margin="2">
                            <Path
                                Data="{StaticResource FilterIcon}"
                                Fill="{telerik1:GreenResource ResourceKey=MarkerBrush}"
                                Stretch="Fill" />
                            <Path
                                Data="{StaticResource FilterIcon}"
                                Fill="{telerik1:GreenResource ResourceKey=AccentLowBrush}"
                                Stretch="Fill"
                                Visibility="{Binding FunnelFillVisibility}" />
                        </Grid>
                    </Border>
                    <telerik:RadDropDownButton.DropDownContent>
                        <ListBox
                            x:Name="PART_FilterOperatorsListBox"
                            ItemsSource="{Binding AvailableOperatorViewModels}"
                            SelectedItem="{Binding SelectedOperatorViewModel, Mode=TwoWay}" />
                    </telerik:RadDropDownButton.DropDownContent>
                </telerik:RadDropDownButton>
            </Grid>
        </Border>
    </ControlTemplate>
    <Style x:Key="FieldFilterControlStyle" TargetType="grid:FieldFilterControl">
        <Setter Property="Template" Value="{StaticResource FieldFilterControlTemplate}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="MinHeight" Value="28" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="telerik:TouchManager.TouchMode" Value="None" />
    </Style>
    <Style BasedOn="{StaticResource FieldFilterControlStyle}" TargetType="grid:FieldFilterControl" />
    <ControlTemplate x:Key="FrozenColumnsSplitterTemplate" TargetType="grid:FrozenColumnsSplitter">
        <Grid Background="Transparent">
            <Border
                Width="5"
                HorizontalAlignment="Left"
                Background="{TemplateBinding Background}"
                Opacity="0.2" />
            <Border
                HorizontalAlignment="Left"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}" />
        </Grid>
    </ControlTemplate>
    <ControlTemplate x:Key="FrozenColumnsSplitterTemplateRS" TargetType="grid:FrozenColumnsSplitterRightSide">
        <Grid Background="Transparent">
            <Border
                Width="5"
                HorizontalAlignment="Left"
                Background="{TemplateBinding Background}"
                Opacity="0.2" />
            <Border
                HorizontalAlignment="Left"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}" />
        </Grid>
    </ControlTemplate>
    <Style x:Key="FrozenColumnsSplitterStyle" TargetType="grid:FrozenColumnsSplitter">
        <Setter Property="Template" Value="{StaticResource FrozenColumnsSplitterTemplate}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=AccentLowBrush}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=AccentLowBrush}" />
        <Setter Property="BorderThickness" Value="1,0,0,0" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style x:Key="FrozenColumnsSplitterRightSideStyle" TargetType="grid:FrozenColumnsSplitterRightSide">
        <Setter Property="Template" Value="{StaticResource FrozenColumnsSplitterTemplateRS}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=AccentLowBrush}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=AccentLowBrush}" />
        <Setter Property="BorderThickness" Value="1,0,0,0" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource FrozenColumnsSplitterStyle}" TargetType="grid:FrozenColumnsSplitter" />
    <Style BasedOn="{StaticResource FrozenColumnsSplitterRightSideStyle}" TargetType="grid:FrozenColumnsSplitterRightSide" />
    <ControlTemplate x:Key="GridViewAggregateResultCellTemplate" TargetType="grid:GridViewAggregateResultCell">
        <StackPanel
            Margin="{TemplateBinding Padding}"
            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
            Background="{TemplateBinding Background}"
            Orientation="Horizontal">
            <TextBlock
                Margin="4,0,0,0"
                VerticalAlignment="Center"
                Text="{Binding Caption}"
                TextAlignment="Right" />
            <TextBlock
                Margin="4,0,0,0"
                VerticalAlignment="Center"
                Text="{Binding FormattedValue}"
                TextAlignment="Right" />
        </StackPanel>
    </ControlTemplate>
    <Style x:Key="GridViewAggregateResultCellStyle" TargetType="grid:GridViewAggregateResultCell">
        <Setter Property="Template" Value="{StaticResource GridViewAggregateResultCellTemplate}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource GridViewAggregateResultCellStyle}" TargetType="grid:GridViewAggregateResultCell" />
    <Style x:Key="FocusVisualStyle">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle Stroke="{telerik1:GreenResource ResourceKey=MarkerBrush}" StrokeThickness="1" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <ControlTemplate x:Key="GridViewCellTemplate" TargetType="grid:GridViewCell">
        <Grid>
            <Border
                x:Name="PART_CellBorder"
                Margin="0,0,0,1"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding VerticalGridLinesBrush}"
                BorderThickness="{Binding VerticalGridLinesWidth, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource GridLineWidthToThicknessConverter}, ConverterParameter=Right}" />
            <Border
                x:Name="Background_Over"
                Background="{TemplateBinding MouseOverBackground}"
                BorderBrush="{TemplateBinding VerticalGridLinesBrush}"
                BorderThickness="{Binding VerticalGridLinesWidth, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource GridLineWidthToThicknessConverter}, ConverterParameter=Right}"
                Visibility="Collapsed" />
            <Border
                x:Name="Background_Invalid_Unfocused"
                Background="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                Opacity="0.3"
                Visibility="Collapsed" />
            <Border
                x:Name="Background_Selected"
                Margin="0,0,0,1"
                Background="{TemplateBinding SelectedBackground}"
                BorderBrush="{TemplateBinding VerticalGridLinesBrush}"
                BorderThickness="{Binding VerticalGridLinesWidth, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource GridLineWidthToThicknessConverter}, ConverterParameter=Right}"
                Visibility="Collapsed" />
            <Border
                x:Name="Background_Current"
                Margin="0,0,1,1"
                Background="{x:Null}"
                BorderBrush="{TemplateBinding CurrentBorderBrush}"
                BorderThickness="1"
                Visibility="Collapsed" />
            <Border
                x:Name="Background_Invalid"
                Background="{telerik1:GreenResource ResourceKey=MainBrush}"
                BorderBrush="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                BorderThickness="1"
                Visibility="Collapsed">
                <ToolTipService.ToolTip>
                    <ToolTip
                        x:Name="validationTooltip"
                        Content="{TemplateBinding Errors}"
                        Placement="Right"
                        Template="{StaticResource GridViewValidationToolTipTemplate}" />
                </ToolTipService.ToolTip>
                <Grid
                    Width="18"
                    Height="18"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Background="Transparent">
                    <Path
                        Width="8"
                        Height="8"
                        Margin="0,-1,-1,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Data="{StaticResource ArrowTopRight8x8}"
                        Fill="{telerik1:GreenResource ResourceKey=ValidationBrush}" />
                </Grid>
            </Border>
            <ContentControl
                x:Name="PART_ContentPresenter"
                Margin="{TemplateBinding Padding}"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch"
                Content="{TemplateBinding Content}"
                ContentTemplate="{TemplateBinding ContentTemplate}"
                Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=grid:DataCellsPresenter}}"
                IsTabStop="{TemplateBinding IsTabStop}"
                TextBlock.FontFamily="{TemplateBinding FontFamily}"
                TextBlock.FontSize="{TemplateBinding FontSize}" />
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup x:Name="HighlightStates">
                    <VisualState x:Name="NotHighlighted" />
                    <VisualState x:Name="Highlighted">
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames
                                Storyboard.TargetName="PART_CellBorder"
                                Storyboard.TargetProperty="Background"
                                Duration="0">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <SolidColorBrush Color="#8CFFE00B" />
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="PART_ContentPresenter" Property="Opacity" Value="{telerik1:GreenResource ResourceKey=DisabledOpacity}" />
                <Setter TargetName="Background_Current" Property="Opacity" Value="{telerik1:GreenResource ResourceKey=DisabledOpacity}" />
            </Trigger>
            <Trigger Property="IsInEditMode" Value="True">
                <Setter Property="Padding" Value="0" />
                <Setter Property="VerticalContentAlignment" Value="Stretch" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="DisplayVisualCues" Value="True" />
                    <Condition Property="ParentRowValid" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Invalid_Unfocused" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Selected" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Over" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsCurrent" Value="True" />
                    <Condition Property="IsInEditMode" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Current" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <Trigger Property="IsValid" Value="False">
                <Setter TargetName="Background_Invalid" Property="Visibility" Value="Visible" />
            </Trigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=ParentRow.HorizontalGridLinesWidth}" Value="0">
                <Setter TargetName="PART_CellBorder" Property="Margin" Value="0" />
                <Setter TargetName="Background_Selected" Property="Margin" Value="0" />
                <Setter TargetName="Background_Current" Property="Margin" Value="0,0,1,0" />
            </DataTrigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=ParentRow.VerticalGridLinesWidth}" Value="0">
                <Setter TargetName="Background_Current" Property="Margin" Value="0,0,0,1" />
            </DataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=ParentRow.HorizontalGridLinesWidth}" Value="0" />
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=ParentRow.VerticalGridLinesWidth}" Value="0" />
                </MultiDataTrigger.Conditions>
                <Setter TargetName="Background_Current" Property="Margin" Value="0" />
            </MultiDataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="GridViewCellStyle" TargetType="grid:GridViewCell">
        <Setter Property="Template" Value="{StaticResource GridViewCellTemplate}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="SelectedBackground" Value="{telerik1:GreenResource ResourceKey=AccentHighBrush}" />
        <Setter Property="CurrentBorderBrush" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="MouseOverBackground" Value="{telerik1:GreenResource ResourceKey=MouseOverBrush}" />
        <Setter Property="Padding" Value="4,0" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="FontSize" Value="{telerik1:GreenResource ResourceKey=FontSize}" />
        <Setter Property="FontFamily" Value="{telerik1:GreenResource ResourceKey=FontFamily}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisualStyle}" />
    </Style>
    <Style BasedOn="{StaticResource GridViewCellStyle}" TargetType="grid:GridViewCell" />
    <Style x:Key="GridViewMergedCell_FocusVisualStyle">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border
                        BorderBrush="{telerik1:GreenResource ResourceKey=MarkerBrush}"
                        BorderThickness="1"
                        Opacity="0.2" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <ControlTemplate x:Key="GridViewMergedCellTemplate" TargetType="grid:GridViewMergedCell">
        <Grid>
            <Border
                x:Name="PART_MergedCellBorder"
                Margin="0,0,1,1"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}" />
            <Border
                x:Name="Background_Over"
                Grid.Column="2"
                Grid.ColumnSpan="2"
                Margin="0,0,1,1"
                Background="{telerik1:GreenResource ResourceKey=MouseOverBrush}"
                Visibility="Collapsed" />
            <Border
                x:Name="Background_Selected"
                Grid.Column="2"
                Grid.ColumnSpan="2"
                Margin="0,0,1,1"
                Background="{telerik1:GreenResource ResourceKey=AccentHighBrush}"
                Visibility="Collapsed" />
            <Border
                x:Name="Background_Edited"
                Grid.Column="2"
                Grid.ColumnSpan="2"
                Background="Transparent"
                BorderBrush="{telerik1:GreenResource ResourceKey=AccentHighBrush}"
                BorderThickness="1"
                IsHitTestVisible="False"
                Visibility="Collapsed" />
            <Border
                x:Name="Background_Current"
                Grid.Column="2"
                Grid.ColumnSpan="2"
                BorderBrush="{telerik1:GreenResource ResourceKey=SelectedBrush}"
                BorderThickness="1"
                IsHitTestVisible="False"
                Visibility="Collapsed" />
            <ContentPresenter
                x:Name="PART_ContentPresenter"
                Margin="{TemplateBinding Padding}"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                Content="{TemplateBinding Content}"
                ContentTemplate="{TemplateBinding ContentTemplate}"
                TextBlock.Foreground="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        </Grid>
        <ControlTemplate.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Selected" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Over" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <Trigger Property="IsInEditMode" Value="True">
                <Setter TargetName="PART_MergedCellBorder" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="PART_ContentPresenter" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="Background_Over" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="Background_Selected" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="Background_Edited" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="PART_ContentPresenter" Property="Opacity" Value="0.25" />
            </Trigger>
            <Trigger Property="IsCurrent" Value="True">
                <Setter TargetName="Background_Current" Property="Visibility" Value="Visible" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="GridViewMergedCellStyle" TargetType="grid:GridViewMergedCell">
        <Setter Property="Template" Value="{StaticResource GridViewMergedCellTemplate}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Padding" Value="5,0" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="AllowDrop" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource GridViewMergedCell_FocusVisualStyle}" />
    </Style>
    <Style BasedOn="{StaticResource GridViewMergedCellStyle}" TargetType="grid:GridViewMergedCell" />
    <ControlTemplate x:Key="GridViewColumnGroupRowTemplate" TargetType="grid:GridViewColumnGroupRow">
        <Border Background="{TemplateBinding Background}">
            <grid:SelectiveScrollingGrid>
                <grid:DataCellsPresenter x:Name="PART_ColumnGroupsDataCellsPresenter">
                    <grid:DataCellsPresenter.Template>
                        <ControlTemplate TargetType="grid:DataCellsPresenter">
                            <ItemsPresenter />
                        </ControlTemplate>
                    </grid:DataCellsPresenter.Template>
                    <grid:DataCellsPresenter.ItemsPanel>
                        <ItemsPanelTemplate>
                            <grid:CommonColumnHeadersPanel />
                        </ItemsPanelTemplate>
                    </grid:DataCellsPresenter.ItemsPanel>
                </grid:DataCellsPresenter>
            </grid:SelectiveScrollingGrid>
        </Border>
    </ControlTemplate>
    <Style x:Key="GridViewColumnGroupRowStyle" TargetType="grid:GridViewColumnGroupRow">
        <Setter Property="Template" Value="{StaticResource GridViewColumnGroupRowTemplate}" />
        <Setter Property="MinHeight" Value="30" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
    </Style>
    <Style BasedOn="{StaticResource GridViewColumnGroupRowStyle}" TargetType="grid:GridViewColumnGroupRow" />
    <Style x:Key="ColumnHeaderGripperStyle" TargetType="Thumb">
        <Setter Property="Width" Value="6" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="Cursor" Value="SizeWE" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <ControlTemplate x:Key="GridViewHeaderCellTemplate" TargetType="grid:GridViewHeaderCell">
        <Grid x:Name="PART_OuterGrid">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid x:Name="PART_HeaderCellGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="4" />
                </Grid.ColumnDefinitions>
                <Border
                    x:Name="GridViewHeaderCell"
                    Grid.ColumnSpan="5"
                    MinHeight="{TemplateBinding MinHeight}"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
                    <Grid>
                        <Border
                            x:Name="GridViewHeaderCell_Over"
                            Background="{telerik1:GreenResource ResourceKey=MouseOverBrush}"
                            Visibility="Collapsed" />
                        <Border
                            x:Name="GridViewHeaderCell_Selected"
                            Background="{telerik1:GreenResource ResourceKey=AccentHighBrush}"
                            Visibility="Collapsed" />
                    </Grid>
                </Border>
                <ContentPresenter
                    Margin="{TemplateBinding Padding}"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    Content="{TemplateBinding Content}"
                    ContentTemplate="{TemplateBinding ContentTemplate}"
                    TextBlock.Foreground="{TemplateBinding Foreground}" />
                <ContentControl
                    Grid.Column="1"
                    Margin="0,0,2,4"
                    VerticalAlignment="Center"
                    Content="{Binding SortingIndex, Converter={StaticResource SortingIndexConverter}, RelativeSource={RelativeSource TemplatedParent}}"
                    FontSize="{telerik1:GreenResource ResourceKey=FontSizeXS}"
                    Foreground="{telerik1:GreenResource ResourceKey=SelectedBrush}"
                    IsTabStop="{TemplateBinding IsTabStop}"
                    Visibility="{Binding SortingIndex, Converter={StaticResource SortingIndexToVisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}}" />
                <Path
                    x:Name="PART_SortIndicator"
                    Grid.Column="2"
                    Width="6"
                    Height="4"
                    Margin="0,0,0,6"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Data="{StaticResource ArrowUp}"
                    Fill="{telerik1:GreenResource ResourceKey=SelectedBrush}"
                    RenderTransformOrigin="0.5,0.5"
                    SnapsToDevicePixels="True"
                    Stretch="Fill"
                    Visibility="Collapsed" />
                <grid:FilteringDropDown
                    Name="PART_DistinctFilterControl"
                    Grid.Column="3"
                    VerticalAlignment="Center"
                    Visibility="{TemplateBinding FilteringUIVisibility}" />
                <Thumb
                    x:Name="PART_LeftHeaderGripper"
                    Grid.ColumnSpan="5"
                    HorizontalAlignment="Left"
                    IsTabStop="{TemplateBinding IsTabStop}"
                    Style="{StaticResource ColumnHeaderGripperStyle}" />
                <Thumb
                    x:Name="PART_RightHeaderGripper"
                    Grid.ColumnSpan="5"
                    HorizontalAlignment="Right"
                    IsTabStop="{TemplateBinding IsTabStop}"
                    Style="{StaticResource ColumnHeaderGripperStyle}" />
            </Grid>
            <grid:FieldFilterControl
                x:Name="PART_FieldFilterControl"
                Grid.Row="1"
                Visibility="{TemplateBinding FieldFilterControlVisibility}" />
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="GridViewHeaderCell_Over" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="SortingState" Value="Ascending">
                <Setter TargetName="GridViewHeaderCell_Selected" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_SortIndicator" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="SortingState" Value="Descending">
                <Setter TargetName="PART_SortIndicator" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_SortIndicator" Property="RenderTransform">
                    <Setter.Value>
                        <RotateTransform Angle="180" />
                    </Setter.Value>
                </Setter>
                <Setter TargetName="GridViewHeaderCell_Selected" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="SortingState" Value="None">
                <Setter TargetName="PART_SortIndicator" Property="Visibility" Value="Collapsed" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="GridViewHeaderCellStyle" TargetType="grid:GridViewHeaderCell">
        <Setter Property="Template" Value="{StaticResource GridViewHeaderCellTemplate}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="MinHeight" Value="30" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="FontSize" Value="{telerik1:GreenResource ResourceKey=FontSize}" />
        <Setter Property="DropIndicatorBrush" Value="{telerik1:GreenResource ResourceKey=MainBrush}" />
        <Setter Property="DropIndicatorThickness" Value="4" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource GridViewHeaderCellStyle}" TargetType="grid:GridViewHeaderCell" />
    <ControlTemplate x:Key="GridViewFooterCellTemplate" TargetType="grid:GridViewFooterCell">
        <Border
            x:Name="PART_FooterCellBorder"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}">
            <ContentPresenter
                Margin="{TemplateBinding Padding}"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                ContentTemplate="{TemplateBinding ContentTemplate}" />
        </Border>
    </ControlTemplate>
    <Style x:Key="GridViewFooterCellStyle" TargetType="grid:GridViewFooterCell">
        <Setter Property="Template" Value="{StaticResource GridViewFooterCellTemplate}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0,0,1,0" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource GridViewFooterCellStyle}" TargetType="grid:GridViewFooterCell" />
    <ControlTemplate x:Key="GridViewGroupFooterCellTemplate" TargetType="grid:GridViewGroupFooterCell">
        <Border
            x:Name="PART_FooterCellBorder"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}">
            <ContentPresenter
                Margin="{TemplateBinding Padding}"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                ContentTemplate="{TemplateBinding ContentTemplate}" />
        </Border>
    </ControlTemplate>
    <Style x:Key="GridViewGroupFooterCellStyle" TargetType="grid:GridViewGroupFooterCell">
        <Setter Property="Template" Value="{StaticResource GridViewGroupFooterCellTemplate}" />
        <Setter Property="BorderThickness" Value="0,0,1,0" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />

        <!--  Stretch so that the FooterTextAlignment property to be respected  -->
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource GridViewGroupFooterCellStyle}" TargetType="grid:GridViewGroupFooterCell" />
    <Style x:Key="GridViewIndentCellStyle" TargetType="grid:GridViewIndentCell">
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="BorderThickness" Value="0,0,1,0" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="grid:GridViewIndentCell">
                    <Border
                        Width="26"
                        Padding="{TemplateBinding Padding}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style BasedOn="{StaticResource GridViewIndentCellStyle}" TargetType="grid:GridViewIndentCell" />
    <Style x:Key="GridViewFooterIndentCellStyle" TargetType="grid:GridViewFooterIndentCell">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="grid:GridViewFooterIndentCell">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Width" Value="26" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="BorderThickness" Value="0,1,1,1" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    </Style>
    <Style BasedOn="{StaticResource GridViewFooterIndentCellStyle}" TargetType="grid:GridViewFooterIndentCell" />
    <Style x:Key="GridViewGroupFooterIndentCellStyle" TargetType="grid:GridViewGroupFooterIndentCell">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="grid:GridViewGroupFooterIndentCell">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Width" Value="26" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    </Style>
    <Style BasedOn="{StaticResource GridViewGroupFooterIndentCellStyle}" TargetType="grid:GridViewGroupFooterIndentCell" />
    <Style x:Key="GridViewHeaderIndentCellStyle" TargetType="grid:GridViewHeaderIndentCell">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="grid:GridViewHeaderIndentCell">
                    <Border
                        x:Name="OuterBorder"
                        Width="26"
                        Padding="{TemplateBinding Padding}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource GridViewHeaderIndentCellStyle}" TargetType="grid:GridViewHeaderIndentCell" />
    <Style x:Key="RowGripperStyle" TargetType="Thumb">
        <Setter Property="Height" Value="4" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="VerticalAlignment" Value="Bottom" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Cursor" Value="SizeNS" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <ControlTemplate x:Key="GridViewRowTemplate" TargetType="grid:GridViewRow">
        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
            <grid:SelectiveScrollingGrid x:Name="grid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Border
                    x:Name="Background_Main"
                    Grid.Column="2"
                    Grid.ColumnSpan="2"
                    Margin="{TemplateBinding Margin}"
                    Padding="{TemplateBinding Padding}"
                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                    Background="{TemplateBinding Background}" />
                <grid:DetailsPresenter
                    x:Name="PART_DetailsPresenter"
                    Grid.Row="1"
                    Grid.Column="2"
                    Grid.ColumnSpan="2"
                    Margin="0,-1,0,0"
                    BorderThickness="0"
                    DetailsProvider="{TemplateBinding DetailsProvider}" />
                <ContentPresenter
                    x:Name="PART_HierarchyChildPresenter"
                    Grid.Row="2"
                    Grid.Column="2"
                    Grid.ColumnSpan="2"
                    Margin="0,-1,0,0"
                    grid:SelectiveScrollingGrid.SelectiveScrollingClip="True"
                    Visibility="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <Border
                    x:Name="Background_Invalid"
                    Grid.Column="1"
                    Grid.ColumnSpan="4"
                    Background="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                    Opacity="0.3"
                    Visibility="Collapsed" />
                <Border
                    Grid.RowSpan="3"
                    Width="26"
                    grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                    Background="{telerik1:GreenResource ResourceKey=PrimaryBrush}"
                    Visibility="{TemplateBinding RowIndicatorVisibility}" />
                <Border
                    x:Name="PART_RowBorder"
                    Grid.RowSpan="3"
                    Grid.ColumnSpan="4"
                    BorderBrush="{TemplateBinding HorizontalGridLinesBrush}"
                    BorderThickness="{Binding HorizontalGridLinesWidth, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource GridLineWidthToThicknessConverter}, ConverterParameter=Bottom}" />
                <Border
                    x:Name="Background_Over"
                    Grid.ColumnSpan="4"
                    Margin="0,0,0,1"
                    Background="{TemplateBinding MouseOverBackground}"
                    Visibility="Collapsed" />
                <Border
                    x:Name="Background_Selected_Unfocused"
                    Grid.ColumnSpan="4"
                    Margin="0,0,0,1"
                    Background="{telerik1:GreenResource ResourceKey=BasicBrush}"
                    Opacity="0.6"
                    Visibility="Collapsed" />
                <Border
                    x:Name="Background_Selected"
                    Grid.ColumnSpan="4"
                    Margin="0,0,0,1"
                    Background="{TemplateBinding SelectedBackground}"
                    Visibility="Collapsed" />
                <Border
                    x:Name="PART_IndicatorPresenter"
                    Grid.RowSpan="3"
                    Width="26"
                    VerticalAlignment="Stretch"
                    grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                    Background="Transparent"
                    BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                    BorderThickness="0,0,1,0"
                    Visibility="{TemplateBinding RowIndicatorVisibility}">
                    <Grid>
                        <Grid x:Name="GridIndicatorPresenter">
                            <Grid
                                x:Name="NavigatorIndicator"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="Collapsed">
                                <Path
                                    Width="5"
                                    Height="8"
                                    Margin="1,0,0,0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Data="{StaticResource ThinArrowRight5x8}"
                                    Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                                    Stretch="Fill" />
                            </Grid>
                            <Grid
                                x:Name="SelectedNavigatorIndicator"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="Collapsed">
                                <Path
                                    Width="5"
                                    Height="8"
                                    Margin="1,0,0,0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Data="{StaticResource ThinArrowRight5x8}"
                                    Fill="{telerik1:GreenResource ResourceKey=SelectedBrush}"
                                    Stretch="Fill" />
                            </Grid>
                            <Grid
                                x:Name="EditIndicator"
                                Width="16"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="Collapsed">
                                <Path
                                    Height="10"
                                    Data="{StaticResource EditIndicatorIcon}"
                                    Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                                    Stretch="Fill" />
                            </Grid>
                            <Grid
                                x:Name="SelectedEditIndicator"
                                Width="16"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="Collapsed">
                                <Path
                                    Height="10"
                                    Data="{StaticResource EditIndicatorIcon}"
                                    Fill="{telerik1:GreenResource ResourceKey=SelectedBrush}"
                                    Stretch="Fill" />
                            </Grid>
                            <Grid
                                x:Name="InvalidEditableIndicator"
                                Background="Transparent"
                                Visibility="Collapsed">
                                <Grid
                                    Width="16"
                                    Height="10"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                                    <Path
                                        Data="{StaticResource EditIndicatorIcon}"
                                        Fill="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                                        Stretch="Fill" />
                                    <Path
                                        Margin="2"
                                        Data="{StaticResource EditableInvertedIcon}"
                                        Fill="{telerik1:GreenResource ResourceKey=PrimaryBrush}"
                                        Stretch="Fill" />
                                </Grid>
                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        Content="{TemplateBinding Errors}"
                                        Placement="Bottom"
                                        Template="{StaticResource GridViewValidationToolTipTemplate}" />
                                </ToolTipService.ToolTip>
                            </Grid>
                            <Grid
                                x:Name="ErrorIndicator"
                                Width="16"
                                Height="16"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="Collapsed">
                                <Path
                                    Data="{StaticResource ErrorIndicatorIcon}"
                                    Fill="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                                    Stretch="Fill" />
                                <Path
                                    Margin="7,3"
                                    Data="{StaticResource ExclamationMarkIcon}"
                                    Fill="White"
                                    Stretch="Fill" />
                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        Content="{TemplateBinding Errors}"
                                        Placement="Bottom"
                                        Template="{StaticResource GridViewValidationToolTipTemplate}" />
                                </ToolTipService.ToolTip>
                            </Grid>
                            <Thumb x:Name="PART_RowResizer" Style="{StaticResource RowGripperStyle}" />
                        </Grid>
                        <grid:GridViewPinButton
                            x:Name="PinnedRowPresenter"
                            IsPinned="{Binding IsPinned, RelativeSource={RelativeSource TemplatedParent}}"
                            Visibility="Collapsed" />
                    </Grid>
                </Border>
                <grid:DataCellsPresenter x:Name="PART_DataCellsPresenter" Grid.Column="3" />
                <controls:GridViewToggleButton
                    x:Name="PART_HierarchyExpandButton"
                    Grid.Column="2"
                    Width="26"
                    grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                    IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                    IsHitTestVisible="{TemplateBinding IsExpandable}"
                    IsTabStop="{TemplateBinding IsTabStop}"
                    Opacity="{Binding IsExpandable, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToOpacityConverter}}"
                    Visibility="{Binding HasHierarchy, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <grid:IndentPresenter
                    x:Name="PART_IndentPresenter"
                    Grid.RowSpan="4"
                    Grid.Column="1"
                    grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                    IndentLevel="{TemplateBinding IndentLevel}" />
            </grid:SelectiveScrollingGrid>
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup>
                    <VisualState x:Name="Unselected" />
                    <VisualState x:Name="SelectedUnfocused">
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames
                                Storyboard.TargetName="Background_Selected_Unfocused"
                                Storyboard.TargetProperty="Visibility"
                                Duration="0">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Visible</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                            <ObjectAnimationUsingKeyFrames
                                Storyboard.TargetName="Background_Selected"
                                Storyboard.TargetProperty="Visibility"
                                Duration="0">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Collapsed</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </VisualState>
                    <VisualState x:Name="Selected">
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames
                                Storyboard.TargetName="Background_Selected"
                                Storyboard.TargetProperty="Visibility"
                                Duration="0">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Visible</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
        </Border>
        <ControlTemplate.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                    <Condition Property="IsValid" Value="True" />
                    <Condition Property="IsSelected" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Over" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsValid" Value="False" />
                    <Condition Property="IsInEditMode" Value="False" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Invalid" Property="Visibility" Value="Visible" />
                <Setter TargetName="ErrorIndicator" Property="Visibility" Value="Visible" />
                <Setter TargetName="Background_Selected_Unfocused" Property="Background" Value="{x:Null}" />
                <Setter TargetName="Background_Selected" Property="Background" Value="{x:Null}" />
                <Setter TargetName="Background_Main" Property="Visibility" Value="Collapsed" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsValid" Value="False" />
                    <Condition Property="IsInEditMode" Value="True" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Invalid" Property="Visibility" Value="Visible" />
                <Setter TargetName="InvalidEditableIndicator" Property="Visibility" Value="Visible" />
                <Setter TargetName="Background_Selected_Unfocused" Property="Background" Value="{x:Null}" />
                <Setter TargetName="Background_Selected" Property="Background" Value="{x:Null}" />
                <Setter TargetName="Background_Main" Property="Visibility" Value="Collapsed" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True" />
                    <Condition Property="IsInEditMode" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="SelectedEditIndicator" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="False" />
                    <Condition Property="IsInEditMode" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="EditIndicator" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsCurrent" Value="True" />
                    <Condition Property="IsSelected" Value="False" />
                    <Condition Property="IsInEditMode" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="NavigatorIndicator" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsCurrent" Value="True" />
                    <Condition Property="IsSelected" Value="True" />
                    <Condition Property="IsInEditMode" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="SelectedNavigatorIndicator" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding IsEnabled, ElementName=PinnedRowPresenter}" Value="True" />
                    <Condition Binding="{Binding IsMouseOver, ElementName=PART_IndicatorPresenter}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter TargetName="GridIndicatorPresenter" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="PinnedRowPresenter" Property="Visibility" Value="Visible" />
            </MultiDataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding IsPinned, RelativeSource={RelativeSource Self}}" Value="True" />
                    <Condition Binding="{Binding IsEnabled, ElementName=PinnedRowPresenter}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter TargetName="GridIndicatorPresenter" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="PinnedRowPresenter" Property="Visibility" Value="Visible" />
            </MultiDataTrigger>
            <Trigger Property="HorizontalGridLinesWidth" Value="0">
                <Setter TargetName="Background_Over" Property="Margin" Value="0" />
                <Setter TargetName="Background_Selected_Unfocused" Property="Margin" Value="0" />
                <Setter TargetName="Background_Selected" Property="Margin" Value="0" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="GridViewRowStyle" TargetType="grid:GridViewRow">
        <Setter Property="Template" Value="{StaticResource GridViewRowTemplate}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="SelectedBackground" Value="{telerik1:GreenResource ResourceKey=AccentHighBrush}" />
        <Setter Property="MouseOverBackground" Value="{telerik1:GreenResource ResourceKey=MouseOverBrush}" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="AllowDrop" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource GridViewRowStyle}" TargetType="grid:GridViewRow" />
    <ControlTemplate x:Key="GridViewNewRow_ValidationToolTipTemplate" TargetType="ToolTip">
        <Grid
            x:Name="Root"
            Margin="2,0"
            Background="Transparent"
            Opacity="0"
            RenderTransformOrigin="0 0">
            <Grid.RenderTransform>
                <TranslateTransform x:Name="xform" X="-25" />
            </Grid.RenderTransform>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border
                Grid.Column="1"
                MinHeight="20"
                Background="{telerik1:GreenResource ResourceKey=ValidationBrush}">
                <ItemsControl ItemsSource="{TemplateBinding Content}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock
                                MinHeight="20"
                                MaxWidth="250"
                                Margin="4"
                                VerticalAlignment="Center"
                                FontFamily="{telerik1:GreenResource ResourceKey=FontFamily}"
                                FontSize="{telerik1:GreenResource ResourceKey=FontSize}"
                                Foreground="{telerik1:GreenResource ResourceKey=SelectedBrush}"
                                Text="{Binding}"
                                TextWrapping="Wrap" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </Border>
        </Grid>
    </ControlTemplate>
    <ControlTemplate x:Key="GridViewNewRowTemplate" TargetType="grid:GridViewNewRow">
        <grid:SelectiveScrollingGrid x:Name="grid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Border
                x:Name="SelectionBackground"
                Grid.Column="2"
                Grid.ColumnSpan="2"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                grid:SelectiveScrollingGrid.SelectiveScrollingClip="True" />
            <Border
                Grid.Column="2"
                Width="26"
                grid:SelectiveScrollingGrid.SelectiveScrollingClip="True"
                grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                BorderBrush="{TemplateBinding VerticalGridLinesBrush}"
                BorderThickness="{Binding VerticalGridLinesWidth, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource GridLineWidthToThicknessConverter}, ConverterParameter=Right}"
                IsHitTestVisible="{Binding IsExpandable, RelativeSource={RelativeSource TemplatedParent}}"
                Opacity="{Binding IsExpandable, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToOpacityConverter}}"
                Visibility="{Binding HasHierarchy, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <Border
                x:Name="PART_AlwaysVisibleNewRow"
                Grid.Column="2"
                Grid.ColumnSpan="2"
                Margin="0"
                grid:SelectiveScrollingGrid.SelectiveScrollingClip="True"
                Background="{TemplateBinding Background}">
                <TextBlock
                    Margin="{TemplateBinding Padding}"
                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                    telerik:LocalizationManager.ResourceKey="GridViewAlwaysVisibleNewRow"
                    FontFamily="{TemplateBinding FontFamily}"
                    FontSize="{TemplateBinding FontSize}"
                    Foreground="{TemplateBinding Foreground}" />
            </Border>
            <Border
                Grid.RowSpan="3"
                Width="26"
                grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                Background="{telerik1:GreenResource ResourceKey=PrimaryBrush}"
                Visibility="{TemplateBinding RowIndicatorVisibility}" />
            <Border
                x:Name="PART_RowBorder"
                Grid.RowSpan="4"
                Grid.Column="1"
                Grid.ColumnSpan="4"
                VerticalAlignment="Bottom"
                grid:SelectiveScrollingGrid.SelectiveScrollingClip="True"
                BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                BorderThickness="0,0,0,1" />
            <grid:DetailsPresenter
                x:Name="PART_DetailsPresenter"
                Grid.Row="1"
                Grid.Column="3"
                DetailsProvider="{TemplateBinding DetailsProvider}" />
            <Border
                x:Name="ValidationLayer"
                Grid.ColumnSpan="4"
                Background="{telerik1:GreenResource ResourceKey=MainBrush}"
                Visibility="Collapsed" />
            <Border
                x:Name="PART_IndicatorPresenter"
                Grid.RowSpan="2"
                Width="26"
                VerticalAlignment="Stretch"
                grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                Background="Transparent"
                BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                BorderThickness="0,0,1,1"
                Visibility="{TemplateBinding RowIndicatorVisibility}">
                <Grid>
                    <Grid
                        x:Name="NewRowIndicator"
                        Width="15"
                        Height="11"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                        <Path
                            Height="4"
                            Margin="0,0,1,0"
                            VerticalAlignment="Top"
                            Data="{StaticResource NewRowIcon}"
                            Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                            Stretch="Fill" />
                        <Path
                            Width="6"
                            Margin="0,5,0,0"
                            HorizontalAlignment="Right"
                            Data="{StaticResource PlusIcon6x6}"
                            Fill="{telerik1:GreenResource ResourceKey=AccentLowBrush}"
                            Stretch="Fill" />
                    </Grid>
                    <Grid
                        x:Name="EditIndicator"
                        Width="16"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Visibility="Collapsed">
                        <Path
                            Height="10"
                            Data="{StaticResource EditIndicatorIcon}"
                            Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                            Stretch="Fill" />
                    </Grid>
                    <Grid
                        x:Name="InvalidEditableIndicator"
                        Background="Transparent"
                        Visibility="Collapsed">
                        <Grid
                            Width="16"
                            Height="10"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                            <Path
                                Data="{StaticResource EditIndicatorIcon}"
                                Fill="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                                Stretch="Fill" />
                            <Path
                                Margin="2"
                                Data="{StaticResource EditableInvertedIcon}"
                                Fill="{telerik1:GreenResource ResourceKey=MainBrush}"
                                Stretch="Fill" />
                        </Grid>
                        <ToolTipService.ToolTip>
                            <ToolTip
                                Content="{TemplateBinding Errors}"
                                Placement="Bottom"
                                Template="{StaticResource GridViewNewRow_ValidationToolTipTemplate}" />
                        </ToolTipService.ToolTip>
                    </Grid>
                    <Grid
                        x:Name="ErrorIndicator"
                        Width="16"
                        Height="16"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Visibility="Collapsed">
                        <Path
                            Margin="1"
                            Data="{StaticResource ErrorIndicatorCircleIcon}"
                            Stretch="Fill">
                            <Path.Fill>
                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                    <GradientStop Offset="0" Color="#FFFC9999" />
                                    <GradientStop Offset="1" Color="#FFC26666" />
                                </LinearGradientBrush>
                            </Path.Fill>
                        </Path>
                        <Path
                            Margin="7,3"
                            Data="{StaticResource ExclamationMarkIcon}"
                            Fill="White"
                            Stretch="Fill" />
                        <Path Data="{StaticResource ErrorIndicatorIcon}" Stretch="Fill">
                            <Path.Fill>
                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                    <GradientStop Offset="0" Color="Red" />
                                    <GradientStop Offset="1" Color="#FF990000" />
                                </LinearGradientBrush>
                            </Path.Fill>
                        </Path>
                        <ToolTipService.ToolTip>
                            <ToolTip
                                x:Name="validationTooltip"
                                Content="{TemplateBinding Errors}"
                                Placement="Bottom"
                                Template="{StaticResource GridViewNewRow_ValidationToolTipTemplate}" />
                        </ToolTipService.ToolTip>
                    </Grid>
                </Grid>
            </Border>
            <Rectangle
                x:Name="Background_Invalid"
                Grid.ColumnSpan="4"
                grid:SelectiveScrollingGrid.SelectiveScrollingClip="True"
                Stroke="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                StrokeThickness="1"
                Visibility="Collapsed" />
            <grid:DataCellsPresenter
                x:Name="PART_DataCellsPresenter"
                Grid.Column="3"
                Visibility="Collapsed" />
            <grid:IndentPresenter
                x:Name="PART_IndentPresenter"
                Grid.RowSpan="2"
                Grid.Column="1"
                grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                IndentLevel="{TemplateBinding IndentLevel}" />
        </grid:SelectiveScrollingGrid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsInEditMode" Value="True">
                <Setter TargetName="NewRowIndicator" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="EditIndicator" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_DataCellsPresenter" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_AlwaysVisibleNewRow" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="PART_RowBorder" Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
            </Trigger>
            <Trigger Property="IsValid" Value="False">
                <Setter TargetName="Background_Invalid" Property="Visibility" Value="Visible" />
                <Setter TargetName="NewRowIndicator" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="EditIndicator" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="ErrorIndicator" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsInEditMode" Value="False">
                <Setter TargetName="PART_AlwaysVisibleNewRow" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_DataCellsPresenter" Property="Visibility" Value="Collapsed" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="GridViewNewRowStyle" TargetType="grid:GridViewNewRow">
        <Setter Property="MinHeight" Value="26" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="Template" Value="{StaticResource GridViewNewRowTemplate}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource GridViewNewRowStyle}" TargetType="grid:GridViewNewRow" />
    <ControlTemplate x:Key="GridViewGroupHeaderCellTemplate" TargetType="grid:GridViewGroupHeaderCell">
        <Border
            x:Name="PART_CellBorder"
            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}">
            <ContentPresenter
                Margin="{TemplateBinding Padding}"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                Content="{TemplateBinding Content}"
                ContentTemplate="{TemplateBinding ContentTemplate}" />
        </Border>
    </ControlTemplate>
    <Style x:Key="GridViewGroupHeaderCellStyle" TargetType="grid:GridViewGroupHeaderCell">
        <Setter Property="Template" Value="{StaticResource GridViewGroupHeaderCellTemplate}" />
        <Setter Property="Padding" Value="0,0,1,0" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Right" />
        <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource GridViewGroupHeaderCellStyle}" TargetType="grid:GridViewGroupHeaderCell" />
    <DataTemplate x:Key="GroupHeaderTemplate">
        <ContentControl
            Content="{Binding Header}"
            FontWeight="Bold"
            Foreground="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
    </DataTemplate>
    <ControlTemplate x:Key="GridViewGroupToggleButtonTemplate" TargetType="ToggleButton">
        <Border
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}">
            <ContentPresenter />
        </Border>
    </ControlTemplate>
    <Style x:Key="GridViewGroupToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="Template" Value="{StaticResource GridViewGroupToggleButtonTemplate}" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="8,0,0,0" />
    </Style>
    <ControlTemplate x:Key="GridViewGroupRowTemplate" TargetType="grid:GridViewGroupRow">
        <Grid x:Name="PART_GroupExpanderGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition MinHeight="{TemplateBinding MinHeight}" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Border
                x:Name="ToggleButtonBorder"
                Grid.Column="2"
                Grid.ColumnSpan="2"
                MinHeight="{TemplateBinding MinHeight}"
                grid:SelectiveScrollingGrid.SelectiveScrollingClip="True"
                Background="{TemplateBinding Background}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="26" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border
                        Grid.Column="2"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        grid:SelectiveScrollingGrid.SelectiveScrollingClip="True"
                        Visibility="{Binding ShowHeaderAggregates, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <grid:AggregateResultsList
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            ItemsSource="{TemplateBinding AggregateResults}">
                            <grid:AggregateResultsList.ItemTemplate>
                                <DataTemplate>
                                    <grid:GridViewAggregateResultCell AggregateResult="{Binding}" />
                                </DataTemplate>
                            </grid:AggregateResultsList.ItemTemplate>
                            <grid:AggregateResultsList.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </grid:AggregateResultsList.ItemsPanel>
                        </grid:AggregateResultsList>
                    </Border>
                    <Border
                        x:Name="IconOuterBorder"
                        HorizontalAlignment="Stretch"
                        grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                        Background="Transparent"
                        BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                        BorderThickness="0,0,0,1">
                        <Path
                            x:Name="ExpanderButton"
                            Width="6"
                            Height="4"
                            HorizontalAlignment="Center"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Data="{StaticResource ArrowUp}"
                            Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                            RenderTransformOrigin="0.5,0.5">
                            <Path.RenderTransform>
                                <RotateTransform Angle="180" />
                            </Path.RenderTransform>
                        </Path>
                    </Border>
                    <ToggleButton
                        x:Name="HeaderButton"
                        Grid.Column="0"
                        Grid.ColumnSpan="3"
                        grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                        Background="{TemplateBinding Background}"
                        IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                        IsTabStop="{TemplateBinding IsTabStop}"
                        Opacity="0" />
                    <ToggleButton
                        Grid.Column="1"
                        Padding="0,0,10,0"
                        grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                        Background="Transparent"
                        BorderBrush="{x:Null}"
                        Foreground="{TemplateBinding Foreground}"
                        IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                        IsTabStop="{TemplateBinding IsTabStop}"
                        Style="{StaticResource GridViewGroupToggleButtonStyle}">
                        <ContentPresenter
                            Grid.Column="1"
                            Margin="0,0,10,0"
                            VerticalAlignment="Center"
                            Content="{TemplateBinding GroupViewModel}"
                            ContentTemplate="{TemplateBinding GroupHeaderTemplate}"
                            TextBlock.FontFamily="{TemplateBinding FontFamily}"
                            TextBlock.FontSize="{TemplateBinding FontSize}" />
                    </ToggleButton>
                    <Border
                        Grid.Column="1"
                        Grid.ColumnSpan="2"
                        Margin="-1,0,0,0"
                        grid:SelectiveScrollingGrid.SelectiveScrollingClip="True"
                        BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                        BorderThickness="0,0,0,1" />
                </Grid>
            </Border>
            <Border
                x:Name="PART_IndicatorPresenter"
                Width="26"
                VerticalAlignment="Stretch"
                grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                Background="Transparent"
                BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                BorderThickness="0,0,1,1"
                Visibility="{TemplateBinding RowIndicatorVisibility}" />
            <grid:IndentPresenter
                Grid.Column="1"
                grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                Background="{TemplateBinding Background}"
                BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                BorderThickness="0,0,0,1"
                IndentLevel="{TemplateBinding Level}" />
            <Border
                Grid.Column="2"
                Margin="0,-1,0,0"
                VerticalAlignment="Top"
                BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                BorderThickness="0,1,0,0" />
            <Border
                x:Name="Content"
                Grid.Row="1"
                Grid.ColumnSpan="3"
                Visibility="Collapsed">
                <StackPanel>
                    <grid:GridViewVirtualizingPanel x:Name="PART_GridViewVirtualizingPanel" />
                    <grid:GridViewGroupFooterRow x:Name="Footer" />
                </StackPanel>
            </Border>
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup x:Name="ExpandStateGroup">
                    <VisualState x:Name="Expanded">
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Content" Storyboard.TargetProperty="Visibility">
                                <DiscreteObjectKeyFrame KeyTime="00:00:00">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Visible</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </VisualState>
                    <VisualState x:Name="Collapsed" />
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsExpanded" Value="True">
                <Setter TargetName="IconOuterBorder" Property="BorderThickness" Value="0" />
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimationUsingKeyFrames
                                BeginTime="0"
                                Storyboard.TargetName="ExpanderButton"
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)">
                                <EasingDoubleKeyFrame KeyTime="0" Value="0" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimationUsingKeyFrames
                                BeginTime="0"
                                Storyboard.TargetName="ExpanderButton"
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)">
                                <EasingDoubleKeyFrame KeyTime="0" Value="180" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="GridViewGroupRowStyle" TargetType="grid:GridViewGroupRow">
        <Setter Property="Template" Value="{StaticResource GridViewGroupRowTemplate}" />
        <Setter Property="GroupHeaderTemplate" Value="{StaticResource GroupHeaderTemplate}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="AllowDrop" Value="True" />
        <Setter Property="MinHeight" Value="26" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Padding" Value="9,0,0,0" />
        <Setter Property="FontSize" Value="{telerik1:GreenResource ResourceKey=FontSize}" />
        <Setter Property="FontFamily" Value="{telerik1:GreenResource ResourceKey=FontFamily}" />
        <Setter Property="ClipToBounds" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource GridViewGroupRowStyle}" TargetType="grid:GridViewGroupRow" />
    <DataTemplate x:Key="GroupHeaderRowHeaderTemplate">
        <ContentControl
            Content="{Binding Header}"
            FontWeight="Bold"
            Foreground="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
    </DataTemplate>
    <ControlTemplate x:Key="GroupHeaderRowToggleButtonTemplate" TargetType="ToggleButton">
        <Border
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}">
            <ContentPresenter Margin="{TemplateBinding Padding}" />
        </Border>
    </ControlTemplate>
    <Style x:Key="GroupHeaderRowToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="Template" Value="{StaticResource GroupHeaderRowToggleButtonTemplate}" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="0,5,10,5" />
        <Setter Property="BorderBrush" Value="{x:Null}" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>
    <ControlTemplate x:Key="GroupHeaderRowTemplate" TargetType="grid:GroupHeaderRow">
        <Grid x:Name="PART_GroupExpanderGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition MinHeight="{TemplateBinding MinHeight}" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Border
                x:Name="ToggleButtonBorder"
                Grid.Column="2"
                Grid.ColumnSpan="2"
                MinHeight="{TemplateBinding MinHeight}"
                grid:SelectiveScrollingGrid.SelectiveScrollingClip="True"
                Background="{TemplateBinding Background}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="26" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Border
                        Grid.Column="2"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        grid:SelectiveScrollingGrid.SelectiveScrollingClip="True"
                        Visibility="{Binding ShowHeaderAggregates, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <grid:AggregateResultsList
                            x:Name="PART_AggregateResults"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            ItemsSource="{Binding GroupAggregateResults, RelativeSource={RelativeSource TemplatedParent}}">
                            <grid:AggregateResultsList.ItemTemplate>
                                <DataTemplate>
                                    <grid:GridViewAggregateResultCell AggregateResult="{Binding}" />
                                </DataTemplate>
                            </grid:AggregateResultsList.ItemTemplate>
                            <grid:AggregateResultsList.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </grid:AggregateResultsList.ItemsPanel>
                        </grid:AggregateResultsList>
                    </Border>
                    <Grid
                        x:Name="PART_DataCellsPresenterGrid"
                        Grid.Column="1"
                        Grid.ColumnSpan="2"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        grid:SelectiveScrollingGrid.SelectiveScrollingClip="True"
                        IsHitTestVisible="False"
                        Visibility="Collapsed">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <grid:IndentPresenter
                            Grid.Column="0"
                            Background="{x:Null}"
                            BorderBrush="{x:Null}"
                            IndentLevel="{TemplateBinding IndentLevel}"
                            Visibility="Hidden" />
                        <grid:DataCellsPresenter x:Name="PART_DataCellsPresenter" Grid.Column="1">
                            <grid:DataCellsPresenter.Template>
                                <ControlTemplate>
                                    <Grid>
                                        <ItemsPresenter />
                                    </Grid>
                                </ControlTemplate>
                            </grid:DataCellsPresenter.Template>
                        </grid:DataCellsPresenter>
                    </Grid>
                    <Border
                        x:Name="IconOuterBorder"
                        Grid.RowSpan="2"
                        HorizontalAlignment="Stretch"
                        grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                        Background="Transparent"
                        BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                        BorderThickness="0,0,0,1">
                        <Path
                            x:Name="ExpanderButton"
                            Width="6"
                            Height="4"
                            HorizontalAlignment="Center"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Data="{StaticResource ArrowUp}"
                            Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                            RenderTransformOrigin="0.5,0.5">
                            <Path.RenderTransform>
                                <RotateTransform Angle="180" />
                            </Path.RenderTransform>
                        </Path>
                    </Border>
                    <ToggleButton
                        x:Name="HeaderButton"
                        Grid.RowSpan="2"
                        Grid.Column="0"
                        Grid.ColumnSpan="3"
                        grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                        Background="{TemplateBinding Background}"
                        IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                        IsTabStop="{TemplateBinding IsTabStop}"
                        Opacity="0" />
                    <ToggleButton
                        Grid.Column="1"
                        grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                        Background="{TemplateBinding Background}"
                        Foreground="{TemplateBinding Foreground}"
                        IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                        IsTabStop="{TemplateBinding IsTabStop}"
                        Style="{StaticResource GroupHeaderRowToggleButtonStyle}">
                        <ContentPresenter
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Content="{TemplateBinding GroupViewModel}"
                            ContentTemplate="{TemplateBinding GroupHeaderTemplate}"
                            TextBlock.FontFamily="{TemplateBinding FontFamily}"
                            TextBlock.FontSize="{TemplateBinding FontSize}" />
                    </ToggleButton>
                    <Border
                        Grid.RowSpan="2"
                        Grid.Column="1"
                        Grid.ColumnSpan="2"
                        Margin="-1,0,0,0"
                        grid:SelectiveScrollingGrid.SelectiveScrollingClip="True"
                        BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                        BorderThickness="0,0,0,1" />
                </Grid>
            </Border>
            <Border
                x:Name="PART_IndicatorPresenter"
                Width="26"
                VerticalAlignment="Stretch"
                grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                Background="Transparent"
                BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                BorderThickness="0,0,1,1"
                Visibility="{TemplateBinding RowIndicatorVisibility}" />
            <grid:IndentPresenter
                Grid.Column="1"
                grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                Background="{TemplateBinding Background}"
                IndentLevel="{TemplateBinding Level}" />
            <Border
                Grid.Column="2"
                Margin="0,-1,0,0"
                VerticalAlignment="Top"
                BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                BorderThickness="0,1,0,0" />
            <grid:GridViewGroupFooterRow
                x:Name="FooterRowForHeaderAggregates"
                Grid.Row="1"
                Grid.ColumnSpan="3"
                Visibility="{Binding ShowGroupHeaderColumnAggregates, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsExpanded" Value="True">
                <Setter TargetName="IconOuterBorder" Property="BorderThickness" Value="0" />
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimationUsingKeyFrames
                                BeginTime="0"
                                Storyboard.TargetName="ExpanderButton"
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)">
                                <EasingDoubleKeyFrame KeyTime="0" Value="0" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimationUsingKeyFrames
                                BeginTime="0"
                                Storyboard.TargetName="ExpanderButton"
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)">
                                <EasingDoubleKeyFrame KeyTime="0" Value="180" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:RadGridView}}, Path=ColumnAggregatesAlignment, Mode=OneWay}" Value="NextToGroupKey">
                <Setter TargetName="PART_DataCellsPresenterGrid" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_AggregateResults" Property="Visibility" Value="Collapsed" />
            </DataTrigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:RadGridView}}, Path=ColumnAggregatesAlignment, Mode=OneWay}" Value="BelowGroupKey">
                <Setter TargetName="PART_DataCellsPresenterGrid" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_DataCellsPresenterGrid" Property="Grid.Row" Value="1" />
            </DataTrigger>
            <DataTrigger Binding="{Binding AggregateResults.Count, RelativeSource={RelativeSource Self}}" Value="0">
                <Setter TargetName="PART_DataCellsPresenterGrid" Property="Visibility" Value="Collapsed" />
            </DataTrigger>
            <DataTrigger Binding="{Binding ShowHeaderAggregates, RelativeSource={RelativeSource Self}}" Value="False">
                <Setter TargetName="PART_DataCellsPresenterGrid" Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="GroupHeaderRowStyle" TargetType="grid:GroupHeaderRow">
        <Setter Property="Template" Value="{StaticResource GroupHeaderRowTemplate}" />
        <Setter Property="GroupHeaderTemplate" Value="{StaticResource GroupHeaderRowHeaderTemplate}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="AllowDrop" Value="True" />
        <Setter Property="MinHeight" Value="26" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Padding" Value="9,0,0,0" />
        <Setter Property="FontSize" Value="{telerik1:GreenResource ResourceKey=FontSize}" />
        <Setter Property="FontFamily" Value="{telerik1:GreenResource ResourceKey=FontFamily}" />
        <Setter Property="ClipToBounds" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource GroupHeaderRowStyle}" TargetType="grid:GroupHeaderRow" />
    <DataTemplate x:Key="GridViewHeaderIndentCellDataTemplate">
        <grid:GridViewHeaderIndentCell />
    </DataTemplate>
    <ControlTemplate x:Key="GridViewHeaderRowTemplate" TargetType="grid:GridViewHeaderRow">
        <grid:SelectiveScrollingGrid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Border
                x:Name="PART_GridViewHeaderRowBorder"
                Grid.RowSpan="2"
                Grid.ColumnSpan="4"
                Padding="{TemplateBinding Padding}"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}" />
            <Border
                x:Name="CommonHeaderIndent"
                Grid.ColumnSpan="2"
                Margin="-1,0,0,0"
                grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                Background="{telerik1:GreenResource ResourceKey=PrimaryBrush}"
                BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                BorderThickness="0,0,1,0"
                Visibility="{Binding Visibility, ElementName=PART_CommonHeaderPresenter}" />
            <grid:CommonHeaderPresenter x:Name="PART_CommonHeaderPresenter" Grid.Column="3" />
            <Border
                x:Name="PART_IndicatorPresenter"
                Grid.RowSpan="2"
                Width="26"
                grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="0,0,1,1"
                Visibility="{TemplateBinding RowIndicatorVisibility}" />
            <grid:IndentPresenter
                x:Name="PART_IndentPresenter"
                Grid.RowSpan="2"
                Grid.Column="1"
                grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                IndentLevel="{TemplateBinding IndentLevel}"
                ItemTemplate="{StaticResource GridViewHeaderIndentCellDataTemplate}" />
            <Border
                x:Name="PART_HierarchyIndentPresenter"
                Grid.RowSpan="2"
                Grid.Column="2"
                Width="26"
                Background="{telerik1:GreenResource ResourceKey=PrimaryBrush}"
                BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                BorderThickness="0,0,1,1"
                Visibility="{Binding HasHierarchy, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <grid:DataCellsPresenter
                x:Name="PART_DataCellsPresenter"
                Grid.Row="1"
                Grid.Column="3"
                MinHeight="{TemplateBinding MinHeight}" />
        </grid:SelectiveScrollingGrid>
    </ControlTemplate>
    <Style x:Key="GridViewHeaderRowStyle" TargetType="grid:GridViewHeaderRow">
        <Setter Property="Template" Value="{StaticResource GridViewHeaderRowTemplate}" />
        <Setter Property="MinHeight" Value="30" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
    </Style>
    <Style BasedOn="{StaticResource GridViewHeaderRowStyle}" TargetType="grid:GridViewHeaderRow" />
    <DataTemplate x:Key="GridViewFooterIndentCellDataTemplate">
        <grid:GridViewFooterIndentCell />
    </DataTemplate>
    <ControlTemplate x:Key="GridViewFooterRowTemplate" TargetType="grid:GridViewFooterRow">
        <grid:SelectiveScrollingGrid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border
                x:Name="PART_FooterRowBorder"
                Grid.Column="3"
                Padding="{TemplateBinding Padding}"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}" />
            <Border
                x:Name="PART_HierarchyIndent"
                Grid.Column="2"
                Width="26"
                grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                Background="{telerik1:GreenResource ResourceKey=PrimaryBrush}"
                BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                BorderThickness="0,1,0,1"
                Visibility="{Binding HasHierarchy, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <Border
                x:Name="PART_IndicatorPresenter"
                Width="26"
                VerticalAlignment="Stretch"
                grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                Background="{telerik1:GreenResource ResourceKey=PrimaryBrush}"
                BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                BorderThickness="0,1,1,1"
                Visibility="{TemplateBinding RowIndicatorVisibility}" />
            <grid:IndentPresenter
                x:Name="PART_IndentPresenter"
                Grid.Column="1"
                grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                IndentLevel="{TemplateBinding IndentLevel}"
                ItemTemplate="{StaticResource GridViewFooterIndentCellDataTemplate}" />
            <grid:DataCellsPresenter x:Name="PART_DataCellsPresenter" Grid.Column="3" />
        </grid:SelectiveScrollingGrid>
    </ControlTemplate>
    <Style x:Key="GridViewFooterRowStyle" TargetType="grid:GridViewFooterRow">
        <Setter Property="Template" Value="{StaticResource GridViewFooterRowTemplate}" />
        <Setter Property="MinHeight" Value="30" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="BorderThickness" Value="0,1,1,1" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource GridViewFooterRowStyle}" TargetType="grid:GridViewFooterRow" />
    <DataTemplate x:Key="GridViewGroupFooterIndentCellDataTemplate">
        <grid:GridViewGroupFooterIndentCell />
    </DataTemplate>
    <ControlTemplate x:Key="GridViewGroupFooterRowTemplate" TargetType="grid:GridViewGroupFooterRow">
        <grid:SelectiveScrollingGrid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border
                x:Name="PART_FooterRowBorder"
                Grid.Column="3"
                Padding="{TemplateBinding Padding}"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}" />
            <Border
                x:Name="PART_HierarchyIndent"
                Grid.Column="2"
                Width="26"
                grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                Background="{telerik1:GreenResource ResourceKey=PrimaryBrush}"
                BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                BorderThickness="0,1,0,0"
                Visibility="{Binding HasHierarchy, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <Border
                x:Name="PART_IndicatorPresenter"
                Width="26"
                VerticalAlignment="Stretch"
                grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                Background="{telerik1:GreenResource ResourceKey=PrimaryBrush}"
                BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                BorderThickness="0,0,1,1"
                Visibility="{TemplateBinding RowIndicatorVisibility}" />
            <grid:IndentPresenter
                x:Name="PART_IndentPresenter"
                Grid.Column="1"
                grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                IndentLevel="{TemplateBinding IndentLevel}"
                ItemTemplate="{StaticResource GridViewGroupFooterIndentCellDataTemplate}" />
            <grid:DataCellsPresenter x:Name="PART_DataCellsPresenter" Grid.Column="3" />
        </grid:SelectiveScrollingGrid>
    </ControlTemplate>
    <Style x:Key="GridViewGroupFooterRowStyle" TargetType="grid:GridViewGroupFooterRow">
        <Setter Property="Template" Value="{StaticResource GridViewGroupFooterRowTemplate}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="MinHeight" Value="30" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource GridViewGroupFooterRowStyle}" TargetType="grid:GridViewGroupFooterRow" />
    <ControlTemplate x:Key="GridViewCheckBoxTemplate" TargetType="grid:GridViewCheckBox">
        <Grid
            Width="13"
            Height="13"
            HorizontalAlignment="Left"
            VerticalAlignment="Center">
            <Grid Background="Transparent">
                <Border
                    x:Name="PART_RootElement"
                    Background="{telerik1:GreenResource ResourceKey=PrimaryBrush}"
                    BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                    BorderThickness="1"
                    CornerRadius="{telerik1:GreenResource ResourceKey=CornerRadius}" />
                <Border
                    x:Name="IndeterminatePath"
                    Width="9"
                    Height="9"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Background="{telerik1:GreenResource ResourceKey=MarkerBrush}"
                    CornerRadius="1"
                    Visibility="Collapsed" />
                <Path
                    x:Name="CheckedPath"
                    Width="8"
                    Height="7"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Data="{StaticResource OptionMark}"
                    Stretch="Fill"
                    Stroke="{telerik1:GreenResource ResourceKey=MarkerBrush}"
                    StrokeThickness="1.5"
                    Visibility="Collapsed" />
            </Grid>
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup x:Name="CheckStates">
                    <VisualState x:Name="Checked">
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames
                                BeginTime="0"
                                Storyboard.TargetName="CheckedPath"
                                Storyboard.TargetProperty="(UIElement.Visibility)"
                                Duration="0">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Visible</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </VisualState>
                    <VisualState x:Name="Unchecked" />
                    <VisualState x:Name="Indeterminate">
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames
                                BeginTime="0"
                                Storyboard.TargetName="IndeterminatePath"
                                Storyboard.TargetProperty="(UIElement.Visibility)"
                                Duration="0">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Visible</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
        </Grid>
    </ControlTemplate>
    <Style x:Key="GridViewCheckBoxStyle" TargetType="grid:GridViewCheckBox">
        <Setter Property="Template" Value="{StaticResource GridViewCheckBoxTemplate}" />
    </Style>
    <Style BasedOn="{StaticResource GridViewCheckBoxStyle}" TargetType="grid:GridViewCheckBox" />
    <ControlTemplate x:Key="GridViewEditorPresenterValidationToolTipTemplate" TargetType="ToolTip">
        <Grid
            x:Name="Root"
            Margin="2,0,0,0"
            Background="Transparent"
            Opacity="0"
            RenderTransformOrigin="0 0">
            <Grid.RenderTransform>
                <TranslateTransform x:Name="xform" X="-25" />
            </Grid.RenderTransform>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Border
                Grid.Column="1"
                MinHeight="20"
                Padding="4,3,4,3"
                Background="{telerik1:GreenResource ResourceKey=ValidationBrush}">
                <TextBlock
                    MaxWidth="250"
                    VerticalAlignment="Center"
                    FontFamily="{telerik1:GreenResource ResourceKey=FontFamily}"
                    FontSize="{telerik1:GreenResource ResourceKey=FontSize}"
                    Foreground="{telerik1:GreenResource ResourceKey=FixedBrush}"
                    Text="{TemplateBinding Content}"
                    TextWrapping="Wrap" />
            </Border>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsOpen" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard x:Name="OpenAnimation">
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="xform"
                                Storyboard.TargetProperty="X"
                                To="0"
                                Duration="0:0:0.2" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard x:Name="CloseAnimation">
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="xform"
                                Storyboard.TargetProperty="X"
                                To="-25"
                                Duration="0:0:0.2" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
                <Setter TargetName="Root" Property="Opacity" Value="1" />
            </Trigger>
            <Trigger Property="IsOpen" Value="False">
                <Setter TargetName="Root" Property="Opacity" Value="0" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <ControlTemplate x:Key="GridViewEditorPresenterTemplate" TargetType="grid:GridViewEditorPresenter">
        <Grid Background="Transparent">
            <ContentPresenter
                x:Name="ContentPresenter"
                Margin="{TemplateBinding Padding}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
            <Border
                x:Name="ValidationErrorElement"
                BorderBrush="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                BorderThickness="1"
                Visibility="Collapsed">
                <ToolTipService.ToolTip>
                    <ToolTip
                        x:Name="validationTooltip"
                        VerticalContentAlignment="Center"
                        Content="{TemplateBinding ErrorMessage}"
                        Placement="Right"
                        Template="{StaticResource GridViewEditorPresenterValidationToolTipTemplate}" />
                </ToolTipService.ToolTip>
                <Grid
                    Width="32"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Background="Transparent">
                    <Path
                        Margin="0,-1,-1,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Data="{StaticResource ArrowTopRight8x8}"
                        Fill="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                        Stretch="None" />
                </Grid>
            </Border>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsContentValid" Value="False">
                <Setter TargetName="ValidationErrorElement" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsContentValid" Value="True">
                <Setter Property="Margin" Value="1,1,2,2" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="GridViewEditorPresenterStyle" TargetType="grid:GridViewEditorPresenter">
        <Setter Property="Template" Value="{StaticResource GridViewEditorPresenterTemplate}" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Padding" Value="0" />
    </Style>
    <Style BasedOn="{StaticResource GridViewEditorPresenterStyle}" TargetType="grid:GridViewEditorPresenter" />
    <ControlTemplate x:Key="GridViewToggleButtonTemplate" TargetType="controls:GridViewToggleButton">
        <Border
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="Transparent">
            <Border
                Width="9"
                Height="9"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}">
                <Grid>
                    <Path
                        x:Name="plus"
                        Width="5"
                        Height="5"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Data="{StaticResource PlusIcon5x5}"
                        Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                        SnapsToDevicePixels="True"
                        Stretch="Fill" />
                    <Rectangle
                        x:Name="minus"
                        Width="5"
                        Height="1"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                        Opacity="0" />
                </Grid>
            </Border>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="plus" Property="Opacity" Value="0" />
                <Setter TargetName="minus" Property="Opacity" Value="1" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <ControlTemplate x:Key="GridViewToggleButtonArrowTemplate" TargetType="controls:GridViewToggleButton">
        <Border
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="Transparent"
            SnapsToDevicePixels="True"
            UseLayoutRounding="True">
            <Grid
                Width="9"
                Height="9"
                Background="Transparent">
                <Path
                    x:Name="plus"
                    Width="5"
                    Height="8"
                    Margin="0,1,0,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Data="{StaticResource ArrowRight5x9}"
                    Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                    SnapsToDevicePixels="True"
                    Stretch="Fill" />
                <Path
                    x:Name="minus"
                    Width="7"
                    Height="7"
                    Margin="{TemplateBinding Padding}"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Data="{StaticResource ArrowBottomRight7x7}"
                    Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                    Opacity="0"
                    SnapsToDevicePixels="True"
                    Stretch="Fill" />
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="plus" Property="Opacity" Value="0" />
                <Setter TargetName="minus" Property="Opacity" Value="1" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="GridViewToggleButtonStyle" TargetType="controls:GridViewToggleButton">
        <Setter Property="Template" Value="{StaticResource GridViewToggleButtonTemplate}" />
        <Setter Property="PlusMinusTemplate" Value="{StaticResource GridViewToggleButtonTemplate}" />
        <Setter Property="ArrowTemplate" Value="{StaticResource GridViewToggleButtonArrowTemplate}" />
        <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="telerik:TouchManager.TouchMode" Value="None" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource GridViewToggleButtonStyle}" TargetType="controls:GridViewToggleButton" />
    <ControlTemplate x:Key="GridViewGroupPanelTemplate" TargetType="grid:GridViewGroupPanel">
        <Grid>
            <Border
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}" />
            <StackPanel Orientation="Horizontal">
                <TextBlock
                    x:Name="panelText"
                    Margin="{TemplateBinding Padding}"
                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                    telerik:LocalizationManager.ResourceKey="GridViewGroupPanelText"
                    FontFamily="{telerik1:GreenResource ResourceKey=FontFamily}"
                    FontSize="{telerik1:GreenResource ResourceKey=FontSizeXS}"
                    IsHitTestVisible="False" />
                <TextBlock
                    x:Name="panelTextGrouped"
                    Margin="{TemplateBinding Padding}"
                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                    telerik:LocalizationManager.ResourceKey="GridViewGroupPanelTopTextGrouped"
                    FontFamily="{telerik1:GreenResource ResourceKey=FontFamily}"
                    FontSize="{telerik1:GreenResource ResourceKey=FontSizeXS}"
                    IsHitTestVisible="False"
                    Visibility="Collapsed" />
                <ItemsPresenter VerticalAlignment="Center" />
            </StackPanel>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="HasItems" Value="True">
                <Setter TargetName="panelText" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="panelTextGrouped" Property="Visibility" Value="Visible" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <ItemsPanelTemplate x:Key="GridViewGroupPanelItemsPanelTemplate">
        <StackPanel VerticalAlignment="Stretch" Orientation="Horizontal" />
    </ItemsPanelTemplate>
    <Style x:Key="GridViewGroupPanelStyle" TargetType="grid:GridViewGroupPanel">
        <Setter Property="MinHeight" Value="30" />
        <Setter Property="Template" Value="{StaticResource GridViewGroupPanelTemplate}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Padding" Value="4,0,0,0" />
        <Setter Property="Margin" Value="0,0,0,-1" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="ItemsPanel" Value="{StaticResource GridViewGroupPanelItemsPanelTemplate}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource GridViewGroupPanelStyle}" TargetType="grid:GridViewGroupPanel" />
    <ControlTemplate x:Key="CloseButtonTemplate" TargetType="Button">
        <Border
            x:Name="CloseButton"
            Background="{TemplateBinding Background}"
            SnapsToDevicePixels="True"
            UseLayoutRounding="True">
            <Path
                Width="8"
                Height="8"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Data="{StaticResource CloseIcon8x8}"
                Fill="{telerik1:GreenResource ResourceKey=MarkerBrush}"
                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                Stretch="Fill" />
        </Border>
    </ControlTemplate>
    <Style x:Key="ButtonCloseStyle" TargetType="Button">
        <Setter Property="Template" Value="{StaticResource CloseButtonTemplate}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Background" Value="Transparent" />
    </Style>
    <ControlTemplate x:Key="GridViewGroupPanelCellTemplate" TargetType="grid:GridViewGroupPanelCell">
        <Grid UseLayoutRounding="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Border
                x:Name="OuterBorder"
                Grid.ColumnSpan="3"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="{telerik1:GreenResource ResourceKey=CornerRadius}"
                UseLayoutRounding="True" />
            <Border
                x:Name="OuterBorder_Over"
                Grid.ColumnSpan="3"
                Background="{telerik1:GreenResource ResourceKey=MouseOverBrush}"
                CornerRadius="{telerik1:GreenResource ResourceKey=CornerRadius}"
                UseLayoutRounding="True"
                Visibility="Collapsed" />
            <Path
                x:Name="PART_SortIndicator"
                Grid.Column="1"
                Width="6"
                Height="4"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Data="{StaticResource ArrowDown}"
                Fill="{telerik1:GreenResource ResourceKey=MarkerBrush}"
                RenderTransformOrigin="0.5,0.5"
                SnapsToDevicePixels="True"
                Stretch="Fill"
                Visibility="Collapsed" />
            <ContentControl
                x:Name="ContentElement"
                Margin="{TemplateBinding Padding}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                Content="{TemplateBinding Content}"
                Foreground="{TemplateBinding Foreground}"
                IsTabStop="{TemplateBinding IsTabStop}">
                <ContentControl.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform />
                        <SkewTransform />
                        <RotateTransform />
                        <TranslateTransform />
                    </TransformGroup>
                </ContentControl.RenderTransform>
            </ContentControl>
            <Button
                x:Name="PART_CloseButton"
                Grid.Column="2"
                Margin="7,0,4,0"
                VerticalAlignment="Center"
                Opacity="0"
                Style="{StaticResource ButtonCloseStyle}" />
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualStateGroup.Transitions>
                        <VisualTransition GeneratedDuration="0:0:0.2" To="MouseOver" />
                        <VisualTransition GeneratedDuration="0:0:0.2" To="Normal" />
                    </VisualStateGroup.Transitions>
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="MouseOver">
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="OuterBorder_Over" Storyboard.TargetProperty="Visibility">
                                <DiscreteObjectKeyFrame KeyTime="0:0:0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Visible</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                            <DoubleAnimationUsingKeyFrames
                                BeginTime="0"
                                Storyboard.TargetName="ContentElement"
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)"
                                Duration="0">
                                <EasingDoubleKeyFrame KeyTime="0" Value="-7" />
                            </DoubleAnimationUsingKeyFrames>
                            <DoubleAnimationUsingKeyFrames
                                BeginTime="0"
                                Storyboard.TargetName="PART_CloseButton"
                                Storyboard.TargetProperty="Opacity"
                                Duration="0:001">
                                <DiscreteDoubleKeyFrame KeyTime="0" Value="1" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
        </Grid>
        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding Path=SortDirection}" Value="Ascending">
                <Setter TargetName="PART_SortIndicator" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_SortIndicator" Property="LayoutTransform">
                    <Setter.Value>
                        <ScaleTransform ScaleX="1" ScaleY="-1" />
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=SortDirection}" Value="Descending">
                <Setter TargetName="PART_SortIndicator" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_SortIndicator" Property="LayoutTransform">
                    <Setter.Value>
                        <ScaleTransform ScaleX="1" ScaleY="1" />
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=SortDirection}" Value="{x:Null}">
                <Setter TargetName="PART_SortIndicator" Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="GridViewGroupPanelCellStyle" TargetType="grid:GridViewGroupPanelCell">
        <Setter Property="Template" Value="{StaticResource GridViewGroupPanelCellTemplate}" />
        <Setter Property="MinHeight" Value="22" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="20,2" />
        <Setter Property="FontFamily" Value="{telerik1:GreenResource ResourceKey=FontFamily}" />
        <Setter Property="FontSize" Value="{telerik1:GreenResource ResourceKey=FontSize}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource GridViewGroupPanelCellStyle}" TargetType="grid:GridViewGroupPanelCell" />
    <telerik:InvertedBooleanToOpacityConverter x:Key="InvertedBooleanToOpacityConverter" />
    <ControlTemplate x:Key="GridViewGroupPanelItemTemplate" TargetType="grid:GridViewGroupPanelItem">
        <StackPanel
            x:Name="PART_GroupPanelItemStackPanel"
            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
            Background="{TemplateBinding Background}"
            Orientation="Horizontal">
            <Grid HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Path
                    x:Name="PART_InsertionPoint"
                    Width="6"
                    Height="4"
                    Margin="3,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Data="{StaticResource ArrowDown}"
                    Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                    SnapsToDevicePixels="True"
                    Stretch="Fill"
                    Visibility="{TemplateBinding InsertionPointVisibility}" />
                <Grid
                    x:Name="PART_ConnectingLine"
                    Grid.Row="1"
                    Opacity="{Binding IsFirst, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource InvertedBooleanToOpacityConverter}}">
                    <Path
                        Width="4"
                        Height="6"
                        Margin="3,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Data="{StaticResource ArrowRight}"
                        Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                        SnapsToDevicePixels="True"
                        Stretch="Fill"
                        Stroke="{x:Null}" />
                </Grid>
            </Grid>
            <grid:GridViewGroupPanelCell
                x:Name="PART_Cell"
                Margin="0,0,0,1"
                Content="{TemplateBinding Content}"
                SortDirection="{Binding Path=SortDirection}" />
            <Path
                x:Name="PART_RightInsertionPoint"
                Width="6"
                Height="4"
                Margin="3,0"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Data="{StaticResource ArrowDown}"
                Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                SnapsToDevicePixels="True"
                Stretch="Fill"
                Visibility="{TemplateBinding RightInsertionPointVisibility}" />
        </StackPanel>
    </ControlTemplate>
    <Style x:Key="GridViewGroupPanelItemStyle" TargetType="grid:GridViewGroupPanelItem">
        <Setter Property="Template" Value="{StaticResource GridViewGroupPanelItemTemplate}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource GridViewGroupPanelItemStyle}" TargetType="grid:GridViewGroupPanelItem" />
    <ControlTemplate x:Key="GridViewLoadingIndicatorTemplate" TargetType="grid:GridViewLoadingIndicator">
        <telerik1:RadBusyIndicator
            BusyContent="{TemplateBinding BusyContent}"
            BusyContentTemplate="{TemplateBinding BusyContentTemplate}"
            IsBusy="{TemplateBinding IsBusy}"
            IsIndeterminate="{TemplateBinding IsIndeterminate}"
            ProgressValue="{TemplateBinding ProgressValue}" />
    </ControlTemplate>
    <Style x:Key="GridViewLoadingIndicatorStyle" TargetType="grid:GridViewLoadingIndicator">
        <Setter Property="Template" Value="{StaticResource GridViewLoadingIndicatorTemplate}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
    </Style>
    <Style BasedOn="{StaticResource GridViewLoadingIndicatorStyle}" TargetType="grid:GridViewLoadingIndicator" />
    <ControlTemplate x:Key="GridViewScrollViewerTemplate" TargetType="grid:GridViewScrollViewer">
        <Grid x:Name="PART_RootPanel" Background="{TemplateBinding Background}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ScrollContentPresenter
                x:Name="PART_ScrollContentPresenter"
                Grid.Row="2"
                CanContentScroll="{TemplateBinding CanContentScroll}" />
            <ScrollBar
                x:Name="PART_VerticalScrollBar"
                Grid.RowSpan="4"
                Grid.Column="1"
                IsTabStop="False"
                Maximum="{TemplateBinding ScrollableHeight}"
                Orientation="Vertical"
                ViewportSize="{TemplateBinding ViewportHeight}"
                Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                Value="{TemplateBinding VerticalOffset}" />
            <ScrollBar
                x:Name="PART_HorizontalScrollBar"
                Grid.Row="4"
                IsTabStop="False"
                Maximum="{TemplateBinding ScrollableWidth}"
                Orientation="Horizontal"
                ViewportSize="{TemplateBinding ViewportWidth}"
                Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                Value="{TemplateBinding HorizontalOffset}" />
            <Border
                Grid.Row="4"
                Grid.Column="1"
                Background="{telerik1:GreenResource ResourceKey=AlternativeBrush}" />
        </Grid>
    </ControlTemplate>
    <Style x:Key="GridViewScrollViewerStyle" TargetType="grid:GridViewScrollViewer">
        <Setter Property="Template" Value="{StaticResource GridViewScrollViewerTemplate}" />
    </Style>
    <Style BasedOn="{StaticResource GridViewScrollViewerStyle}" TargetType="grid:GridViewScrollViewer" />
    <ControlTemplate x:Key="IndentPresenterTemplate" TargetType="grid:IndentPresenter">
        <Grid>
            <ItemsPresenter />
        </Grid>
    </ControlTemplate>
    <DataTemplate x:Key="IndentPresenterItemTemplate">
        <grid:GridViewIndentCell />
    </DataTemplate>
    <ItemsPanelTemplate x:Key="IndentPresenterItemsPanelTemplate">
        <StackPanel Orientation="Horizontal" />
    </ItemsPanelTemplate>
    <Style x:Key="IndentPresenterStyle" TargetType="grid:IndentPresenter">
        <Setter Property="Template" Value="{StaticResource IndentPresenterTemplate}" />
        <Setter Property="ItemTemplate" Value="{StaticResource IndentPresenterItemTemplate}" />
        <Setter Property="ItemsPanel" Value="{StaticResource IndentPresenterItemsPanelTemplate}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource IndentPresenterStyle}" TargetType="grid:IndentPresenter" />
    <ControlTemplate x:Key="RadGridViewTemplate" TargetType="controls:RadGridView">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="0" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <grid:GridViewSearchPanel
                x:Name="PART_SearchPanel"
                Grid.ColumnSpan="4"
                Background="{TemplateBinding SearchPanelBackground}"
                Foreground="{TemplateBinding SearchPanelForeground}"
                Visibility="{Binding ShowSearchPanel, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <grid:GridViewGroupPanel
                x:Name="PART_GroupPanel"
                Grid.Row="1"
                Grid.ColumnSpan="4"
                Background="{TemplateBinding GroupPanelBackground}"
                Foreground="{TemplateBinding GroupPanelForeground}" />
            <telerik:RadDropDownButton
                x:Name="PART_GridViewColumnsSelectionButton"
                Grid.Row="1"
                Grid.Column="4"
                MinWidth="22"
                MinHeight="22"
                Padding="0"
                VerticalAlignment="Center"
                DropDownIndicatorVisibility="Collapsed"
                IsTabStop="False"
                Style="{StaticResource ControlPanelItem_DropDownButtonStyle}"
                Visibility="{Binding ColumnsSelectionButtonVisibility, RelativeSource={RelativeSource TemplatedParent}}">
                <ToolTipService.ToolTip>
                    <ToolTip telerik:LocalizationManager.ResourceKey="GridViewColumnsSelectionButtonTooltip" Placement="Right" />
                </ToolTipService.ToolTip>
                <telerik:RadDropDownButton.Content>
                    <Path
                        Width="16"
                        Height="17"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Data="M14.7,9 L16,10.2 L10.1,17 L6,13 L7.2,11.8 L10.1,14.6 z M12,4 L16,4 L16,8 L12,8 z M6,4 L10,4 L10,11 L6,11 z M0,4 L4,4 L4,16 L0,16 z M12,0 L16,0 L16,2 L12,2 z M6,0 L10,0 L10,2 L6,2 z M0,0 L4,0 L4,2 L0,2 z"
                        Fill="{Binding RelativeSource={RelativeSource AncestorType={x:Type telerik:RadDropDownButton}}, Path=Foreground}"
                        Stretch="Fill"
                        UseLayoutRounding="True" />
                </telerik:RadDropDownButton.Content>
                <telerik:RadDropDownButton.DropDownContent>
                    <telerik:RadListBox
                        MaxWidth="200"
                        MaxHeight="250"
                        Padding="3,0"
                        Background="Transparent"
                        BorderThickness="0"
                        ItemsSource="{Binding Columns}">
                        <telerik:RadListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel />
                            </ItemsPanelTemplate>
                        </telerik:RadListBox.ItemsPanel>
                        <telerik:RadListBox.ItemContainerStyle>
                            <Style TargetType="telerik:RadListBoxItem">
                                <Setter Property="Padding" Value="4,3,3,3" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="telerik:RadListBoxItem">
                                            <CheckBox
                                                Padding="{TemplateBinding Padding}"
                                                Content="{Binding Converter={StaticResource ColumnHeaderConverter}}"
                                                IsChecked="{Binding IsSelected, Mode=TwoWay}" />
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </telerik:RadListBox.ItemContainerStyle>
                    </telerik:RadListBox>
                </telerik:RadDropDownButton.DropDownContent>
            </telerik:RadDropDownButton>
            <Border
                x:Name="PART_MasterGridContainer"
                Grid.Row="2"
                Grid.ColumnSpan="4"
                Padding="{TemplateBinding Padding}"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}" />
            <ItemsControl
                Name="PART_ControlPanelItemsControl"
                Grid.Row="1"
                Grid.Column="2"
                HorizontalAlignment="Right"
                ItemsSource="{TemplateBinding ControlPanelItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <grid:ControlPanelItemControl />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <grid:GridViewScrollViewer
                x:Name="PART_ItemsScrollViewer"
                Grid.Row="2"
                Grid.RowSpan="2"
                Grid.ColumnSpan="4"
                Margin="1"
                CanContentScroll="True">
                <grid:GridViewScrollViewer.HeaderRow>
                    <grid:GridViewHeaderRow x:Name="PART_HeaderRow" IndentLevel="{TemplateBinding GroupCount}" />
                </grid:GridViewScrollViewer.HeaderRow>
                <grid:GridViewScrollViewer.NewRow>
                    <grid:GridViewNewRow Name="PART_AddNewRow" IndentLevel="{TemplateBinding GroupCount}" />
                </grid:GridViewScrollViewer.NewRow>
                <grid:GridViewScrollViewer.FooterRow>
                    <grid:GridViewFooterRow x:Name="PART_FooterRow" IndentLevel="{TemplateBinding GroupCount}" />
                </grid:GridViewScrollViewer.FooterRow>
                <grid:GridViewVirtualizingPanel x:Name="PART_GridViewVirtualizingPanel" />
            </grid:GridViewScrollViewer>
            <grid:ScrollPositionIndicator
                x:Name="PART_ScrollPositionIndicator"
                Grid.Row="2"
                Grid.ColumnSpan="4"
                Margin="0,0,28,0"
                HorizontalAlignment="Right"
                ContentTemplate="{TemplateBinding ScrollPositionIndicatorTemplate}"
                IsHitTestVisible="False"
                Visibility="{Binding IsScrolling, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <Border
                x:Name="PART_FrozenColumnsPreview"
                Grid.Row="2"
                Grid.RowSpan="3"
                Width="6"
                HorizontalAlignment="Left"
                VerticalAlignment="Stretch"
                Background="{telerik1:GreenResource ResourceKey=AccentLowBrush}"
                Opacity="0.1"
                Visibility="Collapsed">
                <Border
                    Width="5"
                    HorizontalAlignment="Right"
                    BorderBrush="{telerik1:GreenResource ResourceKey=AccentLowBrush}"
                    BorderThickness="1,0,0,0" />
            </Border>
            <Border
                x:Name="PART_FrozenColumnsPreviewRight"
                Grid.Row="2"
                Grid.RowSpan="3"
                Width="6"
                HorizontalAlignment="Left"
                VerticalAlignment="Stretch"
                Background="{telerik1:GreenResource ResourceKey=AccentLowBrush}"
                Opacity="0.1"
                Visibility="Collapsed">
                <Border
                    Width="5"
                    HorizontalAlignment="Right"
                    BorderBrush="{telerik1:GreenResource ResourceKey=AccentLowBrush}"
                    BorderThickness="1,0,0,0" />
            </Border>
            <grid:GridViewLoadingIndicator
                x:Name="PART_GridViewLoadingIndicator"
                Grid.RowSpan="4"
                Grid.ColumnSpan="4"
                BusyContent="{Binding BusyIndicatorContent, RelativeSource={RelativeSource TemplatedParent}}"
                BusyContentTemplate="{Binding BusyIndicatorContentTemplate, RelativeSource={RelativeSource TemplatedParent}}"
                IsBusy="{TemplateBinding IsBusy}"
                IsIndeterminate="{Binding BusyIndicatorIsIndeterminate, RelativeSource={RelativeSource TemplatedParent}}"
                ProgressValue="{Binding BusyIndicatorProgressValue, RelativeSource={RelativeSource TemplatedParent}}"
                Visibility="Collapsed" />
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup x:Name="GroupPanelVisibility">
                    <VisualState x:Name="GroupPanelVisible">
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PART_GroupPanel" Storyboard.TargetProperty="Visibility">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Visible</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </VisualState>
                    <VisualState x:Name="GroupPanelCollapsed">
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PART_GroupPanel" Storyboard.TargetProperty="Visibility">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Collapsed</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsBusy" Value="True">
                <Setter TargetName="PART_GridViewLoadingIndicator" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="ShowColumnFooters" Value="false">
                <Setter TargetName="PART_FooterRow" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="ShowColumnHeaders" Value="false">
                <Setter TargetName="PART_HeaderRow" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="NewRowPosition" Value="None">
                <Setter TargetName="PART_AddNewRow" Property="Visibility" Value="Collapsed" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="RadGridViewStyle" TargetType="controls:RadGridView">
        <Setter Property="Template" Value="{StaticResource RadGridViewTemplate}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontFamily" Value="{telerik1:GreenResource ResourceKey=FontFamily}" />
        <Setter Property="FontSize" Value="{telerik1:GreenResource ResourceKey=FontSize}" />
        <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="GroupPanelBackground" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="GroupPanelForeground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="SearchPanelBackground" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="SearchPanelForeground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="VerticalGridLinesBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="HorizontalGridLinesBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="AlternateRowBackground" Value="{telerik1:GreenResource ResourceKey=AlternativeBrush}" />
        <Setter Property="RowHeight" Value="26" />
        <Setter Property="Tag" Value="Green" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    </Style>
    <Style BasedOn="{StaticResource RadGridViewStyle}" TargetType="controls:RadGridView" />
    <Style x:Key="TreeListViewDragCueStyle" TargetType="controls:TreeListViewDragCue">
        <Setter Property="IsHitTestVisible" Value="False" />
        <Setter Property="Padding" Value="4,2" />
        <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=SelectedBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:TreeListViewDragCue">
                    <StackPanel>
                        <Grid
                            MinHeight="22"
                            HorizontalAlignment="Left"
                            Visibility="{TemplateBinding DragPreviewVisibility}">
                            <Grid.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=SelectedBrush}" />
                                </Style>
                            </Grid.Resources>
                            <Border
                                Background="{telerik1:GreenResource ResourceKey=AccentHighBrush}"
                                BorderBrush="{telerik1:GreenResource ResourceKey=AccentHighBrush}"
                                BorderThickness="1"
                                Opacity="0.9">
                                <ItemsPresenter Margin="{TemplateBinding Padding}" />
                            </Border>
                        </Grid>
                        <Border
                            MinHeight="22"
                            Background="{telerik1:GreenResource ResourceKey=PrimaryBrush}"
                            BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                            BorderThickness="1"
                            Opacity="0.9"
                            Visibility="{TemplateBinding DragTooltipVisibility}">
                            <Border.Effect>
                                <DropShadowEffect
                                    BlurRadius="3"
                                    Opacity="0.1"
                                    ShadowDepth="3" />
                            </Border.Effect>
                            <StackPanel Margin="3,0,0,0" Orientation="Horizontal">
                                <ContentPresenter
                                    x:Name="DropImpossibleIconElement"
                                    Content="{TemplateBinding DropImpossibleIcon}"
                                    ContentTemplate="{TemplateBinding DropImpossibleIconTemplate}"
                                    Visibility="Collapsed" />
                                <ContentPresenter
                                    x:Name="DropPossibleIconElement"
                                    Content="{TemplateBinding DropPossibleIcon}"
                                    ContentTemplate="{TemplateBinding DropPossibleIconTemplate}" />
                                <ContentControl
                                    Margin="4,0,0,0"
                                    VerticalAlignment="Center"
                                    Content="{TemplateBinding DragActionContent}"
                                    ContentTemplate="{TemplateBinding DragActionContentTemplate}"
                                    FontFamily="{TemplateBinding FontFamily}"
                                    FontSize="{TemplateBinding FontSize}"
                                    Foreground="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
                                <ContentControl
                                    VerticalAlignment="Center"
                                    Content="{TemplateBinding DragTooltipContent}"
                                    ContentTemplate="{TemplateBinding DragTooltipContentTemplate}"
                                    FontFamily="{TemplateBinding FontFamily}"
                                    FontSize="{TemplateBinding FontSize}"
                                    Foreground="{telerik1:GreenResource ResourceKey=SelectedBrush}" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsDropPossible" Value="False">
                            <Setter TargetName="DropImpossibleIconElement" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="DropImpossibleIconTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Grid UseLayoutRounding="True">
                        <Path
                            Width="12"
                            Height="12"
                            VerticalAlignment="Center"
                            Data="{StaticResource DropImpossibleIcon}"
                            Fill="{telerik1:GreenResource ResourceKey=AccentLowBrush}"
                            SnapsToDevicePixels="True"
                            Stretch="Fill" />
                    </Grid>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="DropPossibleIconTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Grid UseLayoutRounding="True">
                        <Path
                            Width="8"
                            Height="8"
                            VerticalAlignment="Center"
                            Data="{StaticResource PointerRight8x8}"
                            Fill="{telerik1:GreenResource ResourceKey=AccentLowBrush}"
                            SnapsToDevicePixels="True"
                            Stretch="Fill" />
                    </Grid>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource TreeListViewDragCueStyle}" TargetType="controls:TreeListViewDragCue" />
    <ControlTemplate x:Key="RadTreeListViewTemplate" TargetType="controls:RadTreeListView">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="0" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Border
                x:Name="PART_MasterGridContainer"
                Grid.Row="2"
                Grid.ColumnSpan="3"
                Margin="0,-1,0,0"
                Padding="{TemplateBinding Padding}"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}" />
            <ItemsControl
                Name="PART_ControlPanelItemsControl"
                Grid.Row="1"
                Grid.Column="2"
                HorizontalAlignment="Right"
                ItemsSource="{TemplateBinding ControlPanelItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <grid:ControlPanelItemControl />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <grid:GridViewScrollViewer
                x:Name="PART_ItemsScrollViewer"
                Grid.Row="2"
                Grid.RowSpan="2"
                Grid.ColumnSpan="3"
                Margin="1,0,1,1"
                CanContentScroll="True">
                <grid:GridViewScrollViewer.HeaderRow>
                    <grid:GridViewHeaderRow x:Name="PART_HeaderRow" IndentLevel="{TemplateBinding GroupCount}" />
                </grid:GridViewScrollViewer.HeaderRow>
                <grid:GridViewScrollViewer.NewRow>
                    <grid:GridViewNewRow Name="PART_AddNewRow" IndentLevel="{TemplateBinding GroupCount}" />
                </grid:GridViewScrollViewer.NewRow>
                <grid:GridViewScrollViewer.FooterRow>
                    <grid:GridViewFooterRow x:Name="PART_FooterRow" IndentLevel="{TemplateBinding GroupCount}" />
                </grid:GridViewScrollViewer.FooterRow>
                <grid:GridViewVirtualizingPanel x:Name="PART_GridViewVirtualizingPanel" />
            </grid:GridViewScrollViewer>
            <grid:ScrollPositionIndicator
                x:Name="PART_ScrollPositionIndicator"
                Grid.Row="2"
                Grid.ColumnSpan="3"
                Margin="0,0,28,0"
                HorizontalAlignment="Right"
                ContentTemplate="{TemplateBinding ScrollPositionIndicatorTemplate}"
                IsHitTestVisible="False"
                Visibility="{Binding IsScrolling, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <Border
                x:Name="PART_FrozenColumnsPreview"
                Grid.Row="2"
                Grid.RowSpan="3"
                Width="6"
                HorizontalAlignment="Left"
                VerticalAlignment="Stretch"
                Background="{telerik1:GreenResource ResourceKey=AccentLowBrush}"
                Opacity="0.1"
                Visibility="Collapsed">
                <Border
                    Width="5"
                    HorizontalAlignment="Right"
                    BorderBrush="{telerik1:GreenResource ResourceKey=AccentLowBrush}"
                    BorderThickness="1,0,0,0" />
            </Border>
            <Border
                x:Name="PART_FrozenColumnsPreviewRight"
                Grid.Row="2"
                Grid.RowSpan="3"
                Width="6"
                HorizontalAlignment="Left"
                VerticalAlignment="Stretch"
                Background="{telerik1:GreenResource ResourceKey=AccentLowBrush}"
                Opacity="0.1"
                Visibility="Collapsed">
                <Border
                    Width="5"
                    HorizontalAlignment="Right"
                    BorderBrush="{telerik1:GreenResource ResourceKey=AccentLowBrush}"
                    BorderThickness="1,0,0,0" />
            </Border>
            <grid:GridViewLoadingIndicator
                x:Name="PART_GridViewLoadingIndicator"
                Grid.RowSpan="4"
                Grid.ColumnSpan="3"
                BusyContent="{Binding BusyIndicatorContent, RelativeSource={RelativeSource TemplatedParent}}"
                BusyContentTemplate="{Binding BusyIndicatorContentTemplate, RelativeSource={RelativeSource TemplatedParent}}"
                IsBusy="{TemplateBinding IsBusy}"
                IsIndeterminate="{Binding BusyIndicatorIsIndeterminate, RelativeSource={RelativeSource TemplatedParent}}"
                ProgressValue="{Binding BusyIndicatorProgressValue, RelativeSource={RelativeSource TemplatedParent}}"
                Visibility="Collapsed" />
            <Grid
                x:Name="DragBetweenItemsFeedback"
                Grid.Row="2"
                Grid.ColumnSpan="3"
                Height="8"
                Margin="{TemplateBinding Padding}"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                IsHitTestVisible="False"
                Visibility="Collapsed">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Ellipse
                    Width="8"
                    Height="8"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Stroke="{telerik1:GreenResource ResourceKey=AccentHighBrush}"
                    StrokeThickness="2" />
                <Rectangle
                    Grid.Column="1"
                    Height="2"
                    Margin="-2,3,0,0"
                    VerticalAlignment="Top"
                    Fill="{telerik1:GreenResource ResourceKey=AccentHighBrush}"
                    RadiusX="2"
                    RadiusY="2" />
            </Grid>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsBusy" Value="True">
                <Setter TargetName="PART_GridViewLoadingIndicator" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="ShowColumnFooters" Value="false">
                <Setter TargetName="PART_FooterRow" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="ShowColumnHeaders" Value="false">
                <Setter TargetName="PART_HeaderRow" Property="Visibility" Value="Collapsed" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="RadTreeListViewStyle" TargetType="controls:RadTreeListView">
        <Setter Property="Template" Value="{StaticResource RadTreeListViewTemplate}" />
        <Setter Property="FontFamily" Value="{telerik1:GreenResource ResourceKey=FontFamily}" />
        <Setter Property="FontSize" Value="{telerik1:GreenResource ResourceKey=FontSize}" />
        <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="VerticalGridLinesBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="HorizontalGridLinesBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="AlternateRowBackground" Value="{telerik1:GreenResource ResourceKey=AlternativeBrush}" />
        <Setter Property="RowHeight" Value="26" />
        <Setter Property="Tag" Value="Green" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="TreeLinesBrush" Value="{telerik1:GreenResource ResourceKey=StrongBrush}" />
        <Setter Property="TreeLinesStrokeThickness" Value="1" />
    </Style>
    <Style BasedOn="{StaticResource RadTreeListViewStyle}" TargetType="controls:RadTreeListView" />
    <ControlTemplate x:Key="TreeListViewRowTemplate" TargetType="treeList:TreeListViewRow">
        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
            <grid:SelectiveScrollingGrid x:Name="grid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Border
                    x:Name="Background_Main"
                    Grid.Column="2"
                    Grid.ColumnSpan="2"
                    Margin="{TemplateBinding Margin}"
                    Padding="{TemplateBinding Padding}"
                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                    Background="{TemplateBinding Background}" />
                <grid:DetailsPresenter
                    x:Name="PART_DetailsPresenter"
                    Grid.Row="1"
                    Grid.Column="2"
                    Grid.ColumnSpan="2"
                    Margin="0,-1,0,0"
                    BorderThickness="0"
                    DetailsProvider="{TemplateBinding DetailsProvider}" />
                <Border
                    x:Name="Background_Invalid"
                    Grid.Column="1"
                    Grid.ColumnSpan="4"
                    Background="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                    Opacity="0.3"
                    Visibility="Collapsed" />
                <Border
                    Grid.RowSpan="3"
                    Width="26"
                    grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                    Background="{telerik1:GreenResource ResourceKey=PrimaryBrush}"
                    Visibility="{TemplateBinding RowIndicatorVisibility}" />
                <Border
                    x:Name="PART_RowBorder"
                    Grid.RowSpan="3"
                    Grid.ColumnSpan="4"
                    BorderBrush="{TemplateBinding HorizontalGridLinesBrush}"
                    BorderThickness="{Binding HorizontalGridLinesWidth, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource GridLineWidthToThicknessConverter}, ConverterParameter=Bottom}" />
                <Border
                    x:Name="Background_Over"
                    Grid.ColumnSpan="4"
                    Margin="0,0,0,1"
                    Background="{telerik1:GreenResource ResourceKey=MouseOverBrush}"
                    Visibility="Collapsed" />
                <Border
                    x:Name="Background_Selected_Unfocused"
                    Grid.ColumnSpan="4"
                    Margin="0,0,0,1"
                    Background="{telerik1:GreenResource ResourceKey=BasicBrush}"
                    Visibility="Collapsed" />
                <Border
                    x:Name="Background_Selected"
                    Grid.ColumnSpan="4"
                    Margin="0,0,0,1"
                    Background="{telerik1:GreenResource ResourceKey=AccentHighBrush}"
                    Visibility="Collapsed" />
                <controls:GridViewToggleButton
                    x:Name="PART_ExpandButton"
                    Grid.Column="1"
                    Grid.ColumnSpan="2"
                    Width="26"
                    HorizontalAlignment="Left"
                    HorizontalContentAlignment="Center"
                    IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                    IsTabStop="False">
                    <controls:GridViewToggleButton.Style>
                        <Style BasedOn="{StaticResource GridViewToggleButtonStyle}" TargetType="controls:GridViewToggleButton">
                            <Setter Property="PresentationMode" Value="Arrow" />
                        </Style>
                    </controls:GridViewToggleButton.Style>
                </controls:GridViewToggleButton>
                <Border
                    x:Name="PART_IndicatorPresenter"
                    Grid.RowSpan="3"
                    Width="26"
                    VerticalAlignment="Stretch"
                    grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                    BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                    BorderThickness="0,0,1,0"
                    Visibility="{TemplateBinding RowIndicatorVisibility}">
                    <Grid>
                        <Grid
                            x:Name="NavigatorIndicator"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                            <Path
                                Width="5"
                                Height="8"
                                Margin="1,0,0,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Data="{StaticResource ThinArrowRight5x8}"
                                Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                                Stretch="Fill" />
                        </Grid>
                        <Grid
                            x:Name="SelectedNavigatorIndicator"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                            <Path
                                Width="5"
                                Height="8"
                                Margin="1,0,0,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Data="{StaticResource ThinArrowRight5x8}"
                                Fill="{telerik1:GreenResource ResourceKey=SelectedBrush}"
                                Stretch="Fill" />
                        </Grid>
                        <Grid
                            x:Name="EditIndicator"
                            Width="16"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                            <Path
                                Height="10"
                                Data="{StaticResource EditIndicatorIcon}"
                                Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                                Stretch="Fill" />
                        </Grid>
                        <Grid
                            x:Name="SelectedEditIndicator"
                            Width="16"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                            <Path
                                Height="10"
                                Data="{StaticResource EditIndicatorIcon}"
                                Fill="{telerik1:GreenResource ResourceKey=SelectedBrush}"
                                Stretch="Fill" />
                        </Grid>
                        <Grid
                            x:Name="InvalidEditableIndicator"
                            Background="Transparent"
                            Visibility="Collapsed">
                            <Grid
                                Width="16"
                                Height="10"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                                <Path
                                    Data="{StaticResource EditIndicatorIcon}"
                                    Fill="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                                    Stretch="Fill" />
                                <Path
                                    Margin="2"
                                    Data="{StaticResource EditableInvertedIcon}"
                                    Fill="{telerik1:GreenResource ResourceKey=PrimaryBrush}"
                                    Stretch="Fill" />
                            </Grid>
                            <ToolTipService.ToolTip>
                                <ToolTip
                                    Content="{TemplateBinding Errors}"
                                    Placement="Bottom"
                                    Template="{StaticResource GridViewValidationToolTipTemplate}" />
                            </ToolTipService.ToolTip>
                        </Grid>
                        <Grid
                            x:Name="ErrorIndicator"
                            Width="16"
                            Height="16"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                            <Path
                                Data="{StaticResource ErrorIndicatorIcon}"
                                Fill="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                                Stretch="Fill" />
                            <Path
                                Margin="7,3"
                                Data="{StaticResource ExclamationMarkIcon}"
                                Fill="White"
                                Stretch="Fill" />
                            <ToolTipService.ToolTip>
                                <ToolTip
                                    Content="{TemplateBinding Errors}"
                                    Placement="Bottom"
                                    Template="{StaticResource GridViewValidationToolTipTemplate}" />
                            </ToolTipService.ToolTip>
                        </Grid>
                        <Border
                            x:Name="PART_RowResizer"
                            Height="4"
                            VerticalAlignment="Bottom"
                            Background="Transparent"
                            Cursor="SizeNS" />
                    </Grid>
                </Border>
                <grid:DataCellsPresenter
                    x:Name="PART_DataCellsPresenter"
                    Grid.Column="1"
                    Grid.ColumnSpan="2">
                    <grid:DataCellsPresenter.ItemsPanel>
                        <ItemsPanelTemplate>
                            <treeList:TreeListCellsPanel />
                        </ItemsPanelTemplate>
                    </grid:DataCellsPresenter.ItemsPanel>
                </grid:DataCellsPresenter>
            </grid:SelectiveScrollingGrid>
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup>
                    <VisualState x:Name="Unselected" />
                    <VisualState x:Name="SelectedUnfocused">
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames
                                Storyboard.TargetName="Background_Selected_Unfocused"
                                Storyboard.TargetProperty="Visibility"
                                Duration="0">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Visible</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                            <ObjectAnimationUsingKeyFrames
                                Storyboard.TargetName="Background_Selected"
                                Storyboard.TargetProperty="Visibility"
                                Duration="0">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Collapsed</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </VisualState>
                    <VisualState x:Name="Selected">
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames
                                Storyboard.TargetName="Background_Selected"
                                Storyboard.TargetProperty="Visibility"
                                Duration="0">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Visible</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
        </Border>
        <ControlTemplate.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                    <Condition Property="IsValid" Value="True" />
                    <Condition Property="IsSelected" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Over" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsValid" Value="False" />
                    <Condition Property="IsInEditMode" Value="False" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Invalid" Property="Visibility" Value="Visible" />
                <Setter TargetName="ErrorIndicator" Property="Visibility" Value="Visible" />
                <Setter TargetName="Background_Selected_Unfocused" Property="Background" Value="{x:Null}" />
                <Setter TargetName="Background_Main" Property="Visibility" Value="Collapsed" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsValid" Value="False" />
                    <Condition Property="IsInEditMode" Value="True" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Invalid" Property="Visibility" Value="Visible" />
                <Setter TargetName="InvalidEditableIndicator" Property="Visibility" Value="Visible" />
                <Setter TargetName="Background_Selected_Unfocused" Property="Background" Value="{x:Null}" />
                <Setter TargetName="Background_Main" Property="Visibility" Value="Collapsed" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True" />
                    <Condition Property="IsInEditMode" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="SelectedEditIndicator" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="False" />
                    <Condition Property="IsInEditMode" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="EditIndicator" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsCurrent" Value="True" />
                    <Condition Property="IsSelected" Value="False" />
                    <Condition Property="IsInEditMode" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="NavigatorIndicator" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsCurrent" Value="True" />
                    <Condition Property="IsSelected" Value="True" />
                    <Condition Property="IsInEditMode" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="SelectedNavigatorIndicator" Property="Visibility" Value="Visible" />
            </MultiTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="TreeListViewRowStyle" TargetType="treeList:TreeListViewRow">
        <Setter Property="Template" Value="{StaticResource TreeListViewRowTemplate}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="AllowDrop" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource TreeListViewRowStyle}" TargetType="treeList:TreeListViewRow" />
    <ControlTemplate x:Key="NonEditableComboBox" TargetType="input:RadComboBox">
        <Grid x:Name="VisualRoot">
            <ContentPresenter
                x:Name="Content"
                Grid.Column="0"
                Margin="{TemplateBinding Padding}"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                Content="{TemplateBinding SelectionBoxItem}"
                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                IsHitTestVisible="False" />
        </Grid>
    </ControlTemplate>
    <ControlTemplate x:Key="LookupElementTemplate" TargetType="grid:LookupElement">
        <input:RadComboBox
            x:Name="PART_RadComboBox"
            Margin="{TemplateBinding Padding}"
            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
            DisplayMemberPath="{TemplateBinding DisplayMemberPath}"
            EmptyText="{TemplateBinding EmptyText}"
            FontFamily="{TemplateBinding FontFamily}"
            FontSize="{TemplateBinding FontSize}"
            FontWeight="{TemplateBinding FontWeight}"
            Foreground="{TemplateBinding Foreground}"
            IsEditable="False"
            IsSynchronizedWithCurrentItem="False"
            IsTabStop="{TemplateBinding IsTabStop}"
            ItemTemplate="{TemplateBinding ItemTemplate}"
            ItemsSource="{TemplateBinding ItemsSource}"
            NonEditableTemplate="{StaticResource NonEditableComboBox}"
            SelectedValue="{TemplateBinding SelectedValue}"
            SelectedValuePath="{TemplateBinding SelectedValuePath}" />
    </ControlTemplate>
    <Style x:Key="LookupElementStyle" TargetType="grid:LookupElement">
        <Setter Property="Template" Value="{StaticResource LookupElementTemplate}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="IsHitTestVisible" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource LookupElementStyle}" TargetType="grid:LookupElement" />
    <ControlTemplate x:Key="ScrollPositionIndicatorTemplate" TargetType="grid:ScrollPositionIndicator">
        <Border
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}">
            <ContentPresenter
                Margin="{TemplateBinding Padding}"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                Content="{TemplateBinding Content}"
                ContentTemplate="{TemplateBinding ContentTemplate}"
                TextBlock.Foreground="{TemplateBinding Foreground}" />
        </Border>
    </ControlTemplate>
    <Style x:Key="ScrollPositionIndicatorStyle" TargetType="grid:ScrollPositionIndicator">
        <Setter Property="Template" Value="{StaticResource ScrollPositionIndicatorTemplate}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=MainBrush}" />
        <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Padding" Value="10,5" />
    </Style>
    <Style BasedOn="{StaticResource ScrollPositionIndicatorStyle}" TargetType="grid:ScrollPositionIndicator" />
    <ControlTemplate x:Key="GridViewCellCoreTemplate" TargetType="grid:GridViewCell">
        <Grid>
            <Border
                x:Name="PART_CellBorder"
                Margin="0,0,0,1"
                Background="{Binding Background, RelativeSource={RelativeSource TemplatedParent}}"
                BorderBrush="{TemplateBinding VerticalGridLinesBrush}"
                BorderThickness="{Binding VerticalGridLinesWidth, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource GridLineWidthToThicknessConverter}, ConverterParameter=Right}" />
            <Border
                x:Name="Background_Current"
                Margin="0,0,1,1"
                Background="{x:Null}"
                BorderBrush="{TemplateBinding CurrentBorderBrush}"
                BorderThickness="1"
                Visibility="Collapsed" />
            <ContentControl
                x:Name="PART_ContentPresenter"
                Margin="{TemplateBinding Padding}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalContentAlignment="Stretch"
                Content="{TemplateBinding Content}"
                ContentTemplate="{TemplateBinding ContentTemplate}"
                IsTabStop="{TemplateBinding IsTabStop}" />
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsInEditMode" Value="True">
                <Setter Property="Padding" Value="0" />
                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                <Setter TargetName="PART_CellBorder" Property="Background" Value="{telerik1:GreenResource ResourceKey=MainBrush}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="PART_CellBorder" Property="Background" Value="{Binding MouseOverBackground, RelativeSource={RelativeSource TemplatedParent}}" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="PART_CellBorder" Property="Background" Value="{Binding SelectedBackground, RelativeSource={RelativeSource TemplatedParent}}" />
                <Setter TargetName="PART_ContentPresenter" Property="Foreground" Value="{telerik1:GreenResource ResourceKey=SelectedBrush}" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsCurrent" Value="True" />
                    <Condition Property="IsInEditMode" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Current" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="PART_ContentPresenter" Property="Opacity" Value="0.4" />
                <Setter TargetName="Background_Current" Property="Opacity" Value="0.4" />
            </Trigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=ParentRow.HorizontalGridLinesWidth}" Value="0">
                <Setter TargetName="PART_CellBorder" Property="Margin" Value="0" />
                <Setter TargetName="Background_Current" Property="Margin" Value="0,0,1,0" />
            </DataTrigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=ParentRow.VerticalGridLinesWidth}" Value="0">
                <Setter TargetName="Background_Current" Property="Margin" Value="0,0,0,1" />
            </DataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=ParentRow.HorizontalGridLinesWidth}" Value="0" />
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=ParentRow.VerticalGridLinesWidth}" Value="0" />
                </MultiDataTrigger.Conditions>
                <Setter TargetName="Background_Current" Property="Margin" Value="0" />
            </MultiDataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="GridViewCellCoreStyle" TargetType="grid:GridViewCell">
        <Setter Property="Template" Value="{StaticResource GridViewCellCoreTemplate}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="SelectedBackground" Value="{telerik1:GreenResource ResourceKey=AccentHighBrush}" />
        <Setter Property="CurrentBorderBrush" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="MouseOverBackground" Value="{telerik1:GreenResource ResourceKey=MouseOverBrush}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="BorderThickness" Value="0,0,1,0" />
        <Setter Property="Padding" Value="4,0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisualStyle}" />
    </Style>
    <ControlTemplate x:Key="GridViewCellCoreValidationTemplate" TargetType="grid:GridViewCell">
        <Grid>
            <Border
                x:Name="PART_CellBorder"
                Margin="0,0,0,1"
                Background="{Binding Background, RelativeSource={RelativeSource TemplatedParent}}"
                BorderBrush="{TemplateBinding VerticalGridLinesBrush}"
                BorderThickness="{Binding VerticalGridLinesWidth, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource GridLineWidthToThicknessConverter}, ConverterParameter=Right}" />
            <Border
                x:Name="Background_Invalid"
                Background="{telerik1:GreenResource ResourceKey=MainBrush}"
                BorderBrush="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                BorderThickness="1"
                Visibility="Collapsed">
                <ToolTipService.ToolTip>
                    <ToolTip
                        x:Name="validationTooltip"
                        Content="{TemplateBinding Errors}"
                        Placement="Right"
                        Template="{StaticResource GridViewValidationToolTipTemplate}" />
                </ToolTipService.ToolTip>
                <Grid
                    x:Name="validationPath"
                    Width="18"
                    Height="18"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Background="Transparent">
                    <Path
                        Width="8"
                        Height="8"
                        Margin="0,-1,-1,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Data="{StaticResource ArrowTopRight8x8}"
                        Fill="{telerik1:GreenResource ResourceKey=ValidationBrush}" />
                </Grid>
            </Border>
            <Border
                x:Name="Background_Current"
                Margin="0,0,1,1"
                Background="{x:Null}"
                BorderBrush="{TemplateBinding CurrentBorderBrush}"
                BorderThickness="1"
                Visibility="Collapsed" />
            <ContentControl
                x:Name="PART_ContentPresenter"
                Margin="{TemplateBinding Padding}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalContentAlignment="Stretch"
                Content="{TemplateBinding Content}"
                ContentTemplate="{TemplateBinding ContentTemplate}"
                IsTabStop="{TemplateBinding IsTabStop}" />
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsInEditMode" Value="True">
                <Setter Property="Padding" Value="0" />
                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                <Setter TargetName="PART_CellBorder" Property="Background" Value="{telerik1:GreenResource ResourceKey=MainBrush}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="PART_CellBorder" Property="Background" Value="{Binding MouseOverBackground, RelativeSource={RelativeSource TemplatedParent}}" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="PART_CellBorder" Property="Background" Value="{Binding SelectedBackground, RelativeSource={RelativeSource TemplatedParent}}" />
                <Setter TargetName="PART_ContentPresenter" Property="Foreground" Value="{telerik1:GreenResource ResourceKey=SelectedBrush}" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="DisplayVisualCues" Value="True" />
                    <Condition Property="ParentRowValid" Value="false" />
                    <Condition Property="IsValid" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="PART_CellBorder" Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=ValidationBrush}" />
                <Setter TargetName="PART_CellBorder" Property="BorderThickness" Value="1" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsCurrent" Value="True" />
                    <Condition Property="IsInEditMode" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Current" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="PART_ContentPresenter" Property="Opacity" Value="0.4" />
                <Setter TargetName="Background_Current" Property="Opacity" Value="0.4" />
            </Trigger>
            <Trigger Property="IsValid" Value="False">
                <Setter TargetName="Background_Invalid" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_ContentPresenter" Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
            </Trigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=ParentRow.HorizontalGridLinesWidth}" Value="0">
                <Setter TargetName="PART_CellBorder" Property="Margin" Value="0" />
                <Setter TargetName="Background_Current" Property="Margin" Value="0,0,1,0" />
            </DataTrigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=ParentRow.VerticalGridLinesWidth}" Value="0">
                <Setter TargetName="Background_Current" Property="Margin" Value="0,0,0,1" />
            </DataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=ParentRow.HorizontalGridLinesWidth}" Value="0" />
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=ParentRow.VerticalGridLinesWidth}" Value="0" />
                </MultiDataTrigger.Conditions>
                <Setter TargetName="Background_Current" Property="Margin" Value="0" />
            </MultiDataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="GridViewCellCoreValidationStyle" TargetType="grid:GridViewCell">
        <Setter Property="Template" Value="{StaticResource GridViewCellCoreValidationTemplate}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="SelectedBackground" Value="{telerik1:GreenResource ResourceKey=AccentHighBrush}" />
        <Setter Property="CurrentBorderBrush" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="MouseOverBackground" Value="{telerik1:GreenResource ResourceKey=MouseOverBrush}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="BorderThickness" Value="0,0,1,0" />
        <Setter Property="Padding" Value="4,0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisualStyle}" />
    </Style>
    <ControlTemplate x:Key="GridViewRowCoreTemplate" TargetType="grid:GridViewRow">
        <grid:SelectiveScrollingGrid x:Name="grid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border
                x:Name="SelectionBackground"
                Grid.Column="1"
                Grid.ColumnSpan="2"
                grid:SelectiveScrollingGrid.SelectiveScrollingClip="True"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding HorizontalGridLinesBrush}"
                BorderThickness="{Binding HorizontalGridLinesWidth, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource GridLineWidthToThicknessConverter}, ConverterParameter=Bottom}" />
            <grid:DataCellsPresenter x:Name="PART_DataCellsPresenter" Grid.Column="2" />
            <grid:IndentPresenter
                x:Name="PART_IndentPresenter"
                grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                IndentLevel="{TemplateBinding IndentLevel}" />
        </grid:SelectiveScrollingGrid>
        <ControlTemplate.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="DisplayVisualCues" Value="True" />
                    <Condition Property="IsMouseOver" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="SelectionBackground" Property="Background" Value="{Binding MouseOverBackground, RelativeSource={RelativeSource TemplatedParent}}" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="SelectionBackground" Property="Background" Value="{Binding SelectedBackground, RelativeSource={RelativeSource TemplatedParent}}" />
                <Setter TargetName="PART_DataCellsPresenter" Property="Foreground" Value="{telerik1:GreenResource ResourceKey=SelectedBrush}" />
            </MultiTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="GridViewRowCoreStyle" TargetType="grid:GridViewRow">
        <Setter Property="Template" Value="{StaticResource GridViewRowCoreTemplate}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="SelectedBackground" Value="{telerik1:GreenResource ResourceKey=AccentHighBrush}" />
        <Setter Property="MouseOverBackground" Value="{telerik1:GreenResource ResourceKey=MouseOverBrush}" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="AllowDrop" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <ControlTemplate x:Key="GridViewRowCoreValidationTemplate" TargetType="grid:GridViewRow">
        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
            <grid:SelectiveScrollingGrid x:Name="grid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Border
                    x:Name="Background_Main"
                    Grid.Column="2"
                    Grid.ColumnSpan="2"
                    Margin="{TemplateBinding Margin}"
                    Padding="{TemplateBinding Padding}"
                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                    Background="{TemplateBinding Background}" />
                <Border
                    x:Name="Background_Invalid"
                    Grid.Column="1"
                    Grid.ColumnSpan="4"
                    Background="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                    Opacity="0.3"
                    Visibility="Collapsed" />
                <Border
                    Width="26"
                    grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                    Background="{telerik1:GreenResource ResourceKey=PrimaryBrush}"
                    Visibility="{TemplateBinding RowIndicatorVisibility}" />
                <Border
                    x:Name="PART_RowBorder"
                    Grid.ColumnSpan="4"
                    BorderBrush="{TemplateBinding HorizontalGridLinesBrush}"
                    BorderThickness="{Binding HorizontalGridLinesWidth, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource GridLineWidthToThicknessConverter}, ConverterParameter=Bottom}" />
                <Border
                    x:Name="Background_Over"
                    Grid.ColumnSpan="4"
                    Margin="0,0,0,1"
                    Background="{TemplateBinding MouseOverBackground}"
                    Visibility="Collapsed" />
                <Border
                    x:Name="Background_Selected_Unfocused"
                    Grid.ColumnSpan="4"
                    Margin="0,0,0,1"
                    Background="{telerik1:GreenResource ResourceKey=BasicBrush}"
                    Visibility="Collapsed" />
                <Border
                    x:Name="Background_Selected"
                    Grid.ColumnSpan="4"
                    Margin="0,0,0,1"
                    Background="{TemplateBinding SelectedBackground}"
                    Visibility="Collapsed" />
                <Border
                    x:Name="PART_IndicatorPresenter"
                    Width="26"
                    VerticalAlignment="Stretch"
                    grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                    BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                    BorderThickness="0,0,1,0"
                    Visibility="{TemplateBinding RowIndicatorVisibility}">
                    <Grid>
                        <Grid
                            x:Name="NavigatorIndicator"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                            <Path
                                Width="5"
                                Height="8"
                                Margin="1,0,0,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Data="{StaticResource ThinArrowRight5x8}"
                                Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                                Stretch="Fill" />
                        </Grid>
                        <Grid
                            x:Name="SelectedNavigatorIndicator"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                            <Path
                                Width="5"
                                Height="8"
                                Margin="1,0,0,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Data="{StaticResource ThinArrowRight5x8}"
                                Fill="{telerik1:GreenResource ResourceKey=SelectedBrush}"
                                Stretch="Fill" />
                        </Grid>
                        <Grid
                            x:Name="EditIndicator"
                            Width="16"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                            <Path
                                Height="10"
                                Data="{StaticResource EditIndicatorIcon}"
                                Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                                Stretch="Fill" />
                        </Grid>
                        <Grid
                            x:Name="SelectedEditIndicator"
                            Width="16"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                            <Path
                                Height="10"
                                Data="{StaticResource EditIndicatorIcon}"
                                Fill="{telerik1:GreenResource ResourceKey=SelectedBrush}"
                                Stretch="Fill" />
                        </Grid>
                        <Grid
                            x:Name="InvalidEditableIndicator"
                            Background="Transparent"
                            Visibility="Collapsed">
                            <Grid
                                Width="16"
                                Height="10"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                                <Path
                                    Data="{StaticResource EditIndicatorIcon}"
                                    Fill="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                                    Stretch="Fill" />
                                <Path
                                    Margin="2"
                                    Data="{StaticResource EditableInvertedIcon}"
                                    Fill="{telerik1:GreenResource ResourceKey=PrimaryBrush}"
                                    Stretch="Fill" />
                            </Grid>
                            <ToolTipService.ToolTip>
                                <ToolTip
                                    Content="{TemplateBinding Errors}"
                                    Placement="Bottom"
                                    Template="{StaticResource GridViewValidationToolTipTemplate}" />
                            </ToolTipService.ToolTip>
                        </Grid>
                        <Grid
                            x:Name="ErrorIndicator"
                            Width="16"
                            Height="16"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                            <Path
                                Data="{StaticResource ErrorIndicatorIcon}"
                                Fill="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                                Stretch="Fill" />
                            <Path
                                Margin="7,3"
                                Data="{StaticResource ExclamationMarkIcon}"
                                Fill="White"
                                Stretch="Fill" />
                            <ToolTipService.ToolTip>
                                <ToolTip
                                    Content="{TemplateBinding Errors}"
                                    Placement="Bottom"
                                    Template="{StaticResource GridViewValidationToolTipTemplate}" />
                            </ToolTipService.ToolTip>
                        </Grid>
                        <Thumb x:Name="PART_RowResizer" Style="{StaticResource RowGripperStyle}" />
                    </Grid>
                </Border>
                <grid:DataCellsPresenter x:Name="PART_DataCellsPresenter" Grid.Column="3" />
                <grid:IndentPresenter
                    x:Name="PART_IndentPresenter"
                    Grid.Column="1"
                    grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                    IndentLevel="{TemplateBinding IndentLevel}" />
            </grid:SelectiveScrollingGrid>
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup>
                    <VisualState x:Name="Unselected" />
                    <VisualState x:Name="SelectedUnfocused">
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames
                                Storyboard.TargetName="Background_Selected_Unfocused"
                                Storyboard.TargetProperty="Visibility"
                                Duration="0">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Visible</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                            <ObjectAnimationUsingKeyFrames
                                Storyboard.TargetName="Background_Selected"
                                Storyboard.TargetProperty="Visibility"
                                Duration="0">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Collapsed</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </VisualState>
                    <VisualState x:Name="Selected">
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames
                                Storyboard.TargetName="Background_Selected"
                                Storyboard.TargetProperty="Visibility"
                                Duration="0">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Visible</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
        </Border>
        <ControlTemplate.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                    <Condition Property="IsValid" Value="True" />
                    <Condition Property="IsSelected" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Over" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsValid" Value="False" />
                    <Condition Property="IsInEditMode" Value="False" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Invalid" Property="Visibility" Value="Visible" />
                <Setter TargetName="ErrorIndicator" Property="Visibility" Value="Visible" />
                <Setter TargetName="Background_Selected_Unfocused" Property="Background" Value="{x:Null}" />
                <Setter TargetName="Background_Main" Property="Visibility" Value="Collapsed" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsValid" Value="False" />
                    <Condition Property="IsInEditMode" Value="True" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Invalid" Property="Visibility" Value="Visible" />
                <Setter TargetName="InvalidEditableIndicator" Property="Visibility" Value="Visible" />
                <Setter TargetName="Background_Selected_Unfocused" Property="Background" Value="{x:Null}" />
                <Setter TargetName="Background_Main" Property="Visibility" Value="Collapsed" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True" />
                    <Condition Property="IsInEditMode" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="SelectedEditIndicator" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="False" />
                    <Condition Property="IsInEditMode" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="EditIndicator" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsCurrent" Value="True" />
                    <Condition Property="IsSelected" Value="False" />
                    <Condition Property="IsInEditMode" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="NavigatorIndicator" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsCurrent" Value="True" />
                    <Condition Property="IsSelected" Value="True" />
                    <Condition Property="IsInEditMode" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="SelectedNavigatorIndicator" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <Trigger Property="HorizontalGridLinesWidth" Value="0">
                <Setter TargetName="Background_Over" Property="Margin" Value="0" />
                <Setter TargetName="Background_Selected_Unfocused" Property="Margin" Value="0" />
                <Setter TargetName="Background_Selected" Property="Margin" Value="0" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="GridViewRowCoreValidationStyle" TargetType="grid:GridViewRow">
        <Setter Property="Template" Value="{StaticResource GridViewRowCoreValidationTemplate}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="SelectedBackground" Value="{telerik1:GreenResource ResourceKey=AccentHighBrush}" />
        <Setter Property="MouseOverBackground" Value="{telerik1:GreenResource ResourceKey=MouseOverBrush}" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="AllowDrop" Value="True" />
    </Style>
    <ControlTemplate x:Key="TreeListViewRowCoreTemplate" TargetType="treeList:TreeListViewRow">
        <grid:SelectiveScrollingGrid x:Name="grid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border
                x:Name="SelectionBackground"
                Grid.Column="1"
                Grid.ColumnSpan="2"
                grid:SelectiveScrollingGrid.SelectiveScrollingClip="True"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding HorizontalGridLinesBrush}"
                BorderThickness="{Binding HorizontalGridLinesWidth, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource GridLineWidthToThicknessConverter}, ConverterParameter=Bottom}" />
            <controls:GridViewToggleButton
                x:Name="PART_ExpandButton"
                Grid.Column="1"
                Grid.ColumnSpan="2"
                Width="20"
                HorizontalAlignment="Left"
                HorizontalContentAlignment="Center"
                IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                IsTabStop="False"
                PresentationMode="Arrow" />
            <grid:DataCellsPresenter x:Name="PART_DataCellsPresenter" Grid.Column="2">
                <grid:DataCellsPresenter.ItemsPanel>
                    <ItemsPanelTemplate>
                        <treeList:TreeListCellsPanel />
                    </ItemsPanelTemplate>
                </grid:DataCellsPresenter.ItemsPanel>
            </grid:DataCellsPresenter>
        </grid:SelectiveScrollingGrid>
        <ControlTemplate.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="DisplayVisualCues" Value="True" />
                    <Condition Property="IsMouseOver" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="SelectionBackground" Property="Background" Value="{telerik1:GreenResource ResourceKey=MouseOverBrush}" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="SelectionBackground" Property="Background" Value="{telerik1:GreenResource ResourceKey=AccentHighBrush}" />
                <Setter TargetName="PART_DataCellsPresenter" Property="Foreground" Value="{telerik1:GreenResource ResourceKey=SelectedBrush}" />
            </MultiTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="TreeListViewRowCoreStyle" TargetType="treeList:TreeListViewRow">
        <Setter Property="Template" Value="{StaticResource TreeListViewRowCoreTemplate}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="AllowDrop" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <ControlTemplate x:Key="TreeListViewRowCoreValidationTemplate" TargetType="treeList:TreeListViewRow">
        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
            <grid:SelectiveScrollingGrid x:Name="grid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Border
                    x:Name="Background_Main"
                    Grid.Column="2"
                    Grid.ColumnSpan="2"
                    Margin="{TemplateBinding Margin}"
                    Padding="{TemplateBinding Padding}"
                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                    Background="{TemplateBinding Background}" />
                <Border
                    x:Name="Background_Invalid"
                    Grid.Column="1"
                    Grid.ColumnSpan="4"
                    Background="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                    Opacity="0.3"
                    Visibility="Collapsed" />
                <Border
                    Width="26"
                    grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                    Background="{telerik1:GreenResource ResourceKey=PrimaryBrush}"
                    Visibility="{TemplateBinding RowIndicatorVisibility}" />
                <Border
                    x:Name="PART_RowBorder"
                    Grid.ColumnSpan="4"
                    BorderBrush="{TemplateBinding HorizontalGridLinesBrush}"
                    BorderThickness="{Binding HorizontalGridLinesWidth, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource GridLineWidthToThicknessConverter}, ConverterParameter=Bottom}" />
                <Border
                    x:Name="Background_Over"
                    Grid.ColumnSpan="4"
                    Margin="0,0,0,1"
                    Background="{telerik1:GreenResource ResourceKey=MouseOverBrush}"
                    Visibility="Collapsed" />
                <Border
                    x:Name="Background_Selected_Unfocused"
                    Grid.ColumnSpan="4"
                    Margin="0,0,0,1"
                    Background="{telerik1:GreenResource ResourceKey=BasicBrush}"
                    Visibility="Collapsed" />
                <Border
                    x:Name="Background_Selected"
                    Grid.ColumnSpan="4"
                    Margin="0,0,0,1"
                    Background="{telerik1:GreenResource ResourceKey=AccentHighBrush}"
                    Visibility="Collapsed" />
                <controls:GridViewToggleButton
                    x:Name="PART_ExpandButton"
                    Grid.Column="1"
                    Grid.ColumnSpan="2"
                    Width="26"
                    HorizontalAlignment="Left"
                    HorizontalContentAlignment="Center"
                    IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                    IsTabStop="False">
                    <controls:GridViewToggleButton.Style>
                        <Style BasedOn="{StaticResource GridViewToggleButtonStyle}" TargetType="controls:GridViewToggleButton">
                            <Setter Property="PresentationMode" Value="Arrow" />
                        </Style>
                    </controls:GridViewToggleButton.Style>
                </controls:GridViewToggleButton>
                <Border
                    x:Name="PART_IndicatorPresenter"
                    Width="26"
                    VerticalAlignment="Stretch"
                    grid:SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                    BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                    BorderThickness="0,0,1,0"
                    Visibility="{TemplateBinding RowIndicatorVisibility}">
                    <Grid>
                        <Grid
                            x:Name="NavigatorIndicator"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                            <Path
                                Width="5"
                                Height="8"
                                Margin="1,0,0,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Data="{StaticResource ThinArrowRight5x8}"
                                Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                                Stretch="Fill" />
                        </Grid>
                        <Grid
                            x:Name="SelectedNavigatorIndicator"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                            <Path
                                Width="5"
                                Height="8"
                                Margin="1,0,0,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Data="{StaticResource ThinArrowRight5x8}"
                                Fill="{telerik1:GreenResource ResourceKey=SelectedBrush}"
                                Stretch="Fill" />
                        </Grid>
                        <Grid
                            x:Name="EditIndicator"
                            Width="16"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                            <Path
                                Height="10"
                                Data="{StaticResource EditIndicatorIcon}"
                                Fill="{telerik1:GreenResource ResourceKey=StrongBrush}"
                                Stretch="Fill" />
                        </Grid>
                        <Grid
                            x:Name="SelectedEditIndicator"
                            Width="16"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                            <Path
                                Height="10"
                                Data="{StaticResource EditIndicatorIcon}"
                                Fill="{telerik1:GreenResource ResourceKey=SelectedBrush}"
                                Stretch="Fill" />
                        </Grid>
                        <Grid
                            x:Name="InvalidEditableIndicator"
                            Background="Transparent"
                            Visibility="Collapsed">
                            <Grid
                                Width="16"
                                Height="10"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                                <Path
                                    Data="{StaticResource EditIndicatorIcon}"
                                    Fill="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                                    Stretch="Fill" />
                                <Path
                                    Margin="2"
                                    Data="{StaticResource EditableInvertedIcon}"
                                    Fill="{telerik1:GreenResource ResourceKey=PrimaryBrush}"
                                    Stretch="Fill" />
                            </Grid>
                            <ToolTipService.ToolTip>
                                <ToolTip
                                    Content="{TemplateBinding Errors}"
                                    Placement="Bottom"
                                    Template="{StaticResource GridViewValidationToolTipTemplate}" />
                            </ToolTipService.ToolTip>
                        </Grid>
                        <Grid
                            x:Name="ErrorIndicator"
                            Width="16"
                            Height="16"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                            <Path
                                Data="{StaticResource ErrorIndicatorIcon}"
                                Fill="{telerik1:GreenResource ResourceKey=ValidationBrush}"
                                Stretch="Fill" />
                            <Path
                                Margin="7,3"
                                Data="{StaticResource ExclamationMarkIcon}"
                                Fill="White"
                                Stretch="Fill" />
                            <ToolTipService.ToolTip>
                                <ToolTip
                                    Content="{TemplateBinding Errors}"
                                    Placement="Bottom"
                                    Template="{StaticResource GridViewValidationToolTipTemplate}" />
                            </ToolTipService.ToolTip>
                        </Grid>
                        <Border
                            x:Name="PART_RowResizer"
                            Height="4"
                            VerticalAlignment="Bottom"
                            Background="Transparent"
                            Cursor="SizeNS" />
                    </Grid>
                </Border>
                <grid:DataCellsPresenter
                    x:Name="PART_DataCellsPresenter"
                    Grid.Column="1"
                    Grid.ColumnSpan="2">
                    <grid:DataCellsPresenter.ItemsPanel>
                        <ItemsPanelTemplate>
                            <treeList:TreeListCellsPanel />
                        </ItemsPanelTemplate>
                    </grid:DataCellsPresenter.ItemsPanel>
                </grid:DataCellsPresenter>
            </grid:SelectiveScrollingGrid>
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup>
                    <VisualState x:Name="Unselected" />
                    <VisualState x:Name="SelectedUnfocused">
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames
                                Storyboard.TargetName="Background_Selected_Unfocused"
                                Storyboard.TargetProperty="Visibility"
                                Duration="0">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Visible</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                            <ObjectAnimationUsingKeyFrames
                                Storyboard.TargetName="Background_Selected"
                                Storyboard.TargetProperty="Visibility"
                                Duration="0">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Collapsed</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </VisualState>
                    <VisualState x:Name="Selected">
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames
                                Storyboard.TargetName="Background_Selected"
                                Storyboard.TargetProperty="Visibility"
                                Duration="0">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Visible</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
        </Border>
        <ControlTemplate.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                    <Condition Property="IsValid" Value="True" />
                    <Condition Property="IsSelected" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Over" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsValid" Value="False" />
                    <Condition Property="IsInEditMode" Value="False" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Invalid" Property="Visibility" Value="Visible" />
                <Setter TargetName="ErrorIndicator" Property="Visibility" Value="Visible" />
                <Setter TargetName="Background_Selected_Unfocused" Property="Background" Value="{x:Null}" />
                <Setter TargetName="Background_Main" Property="Visibility" Value="Collapsed" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsValid" Value="False" />
                    <Condition Property="IsInEditMode" Value="True" />
                    <Condition Property="DisplayVisualCues" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Background_Invalid" Property="Visibility" Value="Visible" />
                <Setter TargetName="InvalidEditableIndicator" Property="Visibility" Value="Visible" />
                <Setter TargetName="Background_Selected_Unfocused" Property="Background" Value="{x:Null}" />
                <Setter TargetName="Background_Main" Property="Visibility" Value="Collapsed" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True" />
                    <Condition Property="IsInEditMode" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="SelectedEditIndicator" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="False" />
                    <Condition Property="IsInEditMode" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="EditIndicator" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsCurrent" Value="True" />
                    <Condition Property="IsSelected" Value="False" />
                    <Condition Property="IsInEditMode" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="NavigatorIndicator" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsCurrent" Value="True" />
                    <Condition Property="IsSelected" Value="True" />
                    <Condition Property="IsInEditMode" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="SelectedNavigatorIndicator" Property="Visibility" Value="Visible" />
            </MultiTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="TreeListViewRowCoreValidationStyle" TargetType="treeList:TreeListViewRow">
        <Setter Property="Template" Value="{StaticResource TreeListViewRowCoreValidationTemplate}" />
        <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="AllowDrop" Value="True" />
    </Style>
    <ControlTemplate x:Key="ClearSearchValueButtonTemplate" TargetType="telerik:RadButton">
        <Border
            x:Name="ButtonBorder"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}">
            <Path
                Width="8"
                Height="8"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Data="{StaticResource CloseIcon8x8}"
                Fill="{TemplateBinding Foreground}"
                Stretch="Fill"
                Stroke="{x:Null}"
                StrokeThickness="0" />
        </Border>
    </ControlTemplate>
    <Style x:Key="ClearSearchValueButtonStyle" TargetType="telerik:RadButton">
        <Setter Property="Template" Value="{StaticResource ClearSearchValueButtonTemplate}" />
        <Setter Property="Background" Value="#01FFFFFF" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Width" Value="20" />
        <Setter Property="Height" Value="20" />
        <Setter Property="HorizontalAlignment" Value="Right" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{telerik:GreenResource ResourceKey=StrongBrush}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter Property="Foreground" Value="{telerik:GreenResource ResourceKey=MouseOverBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="true">
                <Setter Property="Foreground" Value="{telerik:GreenResource ResourceKey=MouseOverBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <ControlTemplate x:Key="GridViewSearchPanelTemplate" TargetType="grid:GridViewSearchPanel">
        <Border
            Grid.ColumnSpan="2"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}">
            <Grid UseLayoutRounding="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        Margin="{TemplateBinding Padding}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        telerik:LocalizationManager.ResourceKey="GridViewSearchPanelTopText"
                        IsHitTestVisible="False"
                        TextWrapping="Wrap" />
                    <Grid
                        Grid.Column="1"
                        Margin="10,0,10,0"
                        VerticalAlignment="Center">
                        <TextBox
                            x:Name="PART_SearchAsYouTypeTextBox"
                            MinWidth="160"
                            MinHeight="20"
                            MaxWidth="200"
                            Padding="3,3,15,3"
                            telerik:TextBoxBehavior.UpdateTextOnEnter="True"
                            FontFamily="{TemplateBinding FontFamily}"
                            FontSize="{TemplateBinding FontSize}"
                            IsTabStop="{TemplateBinding IsTabStop}"
                            Text="{Binding SearchText, Mode=TwoWay}" />
                        <telerik:RadButton
                            x:Name="ClearButton"
                            Command="searchPanel:GridViewSearchPanelCommands.ClearSearchValue"
                            IsTabStop="False"
                            Style="{StaticResource ClearSearchValueButtonStyle}"
                            Visibility="Collapsed" />
                        <Grid.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ClearButton" Storyboard.TargetProperty="Visibility">
                                            <DiscreteObjectKeyFrame KeyTime="0">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <Visibility>Visible</Visibility>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ClearButton" Storyboard.TargetProperty="Visibility">
                                            <DiscreteObjectKeyFrame KeyTime="0">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <Visibility>Collapsed</Visibility>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Grid.Triggers>
                    </Grid>
                </Grid>
                <telerik:RadPathButton
                    x:Name="CloseButton"
                    Grid.Column="1"
                    Width="22"
                    Height="22"
                    Margin="0,5,5,5"
                    Padding="6"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Command="controls:RadGridViewCommands.CloseSearchPanel"
                    IsBackgroundVisible="False"
                    IsTabStop="False"
                    PathGeometry="{StaticResource CloseIcon8x8}"
                    Visibility="{TemplateBinding CloseButtonVisibility}" />
            </Grid>
        </Border>
    </ControlTemplate>
    <Style x:Key="GridViewSearchPanelStyle" TargetType="grid:GridViewSearchPanel">
        <Setter Property="Template" Value="{StaticResource GridViewSearchPanelTemplate}" />
        <Setter Property="MinHeight" Value="33" />
        <Setter Property="BorderThickness" Value="1,1,1,0" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Background" Value="{telerik:GreenResource ResourceKey=PrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{telerik:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="Foreground" Value="{telerik:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="FontFamily" Value="{telerik:GreenResource ResourceKey=FontFamily}" />
        <Setter Property="FontSize" Value="{telerik1:GreenResource ResourceKey=FontSize}" />
        <Setter Property="Padding" Value="5" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style BasedOn="{StaticResource GridViewSearchPanelStyle}" TargetType="grid:GridViewSearchPanel" />
    <Style x:Key="HighlightTextBlockStyle" TargetType="grid:HighlightTextBlock">
        <Setter Property="HighlightBrush" Value="#FFAE0202" />
    </Style>
    <Style BasedOn="{StaticResource HighlightTextBlockStyle}" TargetType="grid:HighlightTextBlock" />
    <Style x:Key="PinnedButtonStyle" TargetType="Path">
        <Setter Property="Width" Value="7" />
        <Setter Property="Height" Value="9" />
        <Setter Property="Fill" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="Stretch" Value="Fill" />
    </Style>
    <Style x:Key="UnpinnedButtonStyle" TargetType="Path">
        <Setter Property="Width" Value="9" />
        <Setter Property="Height" Value="7" />
        <Setter Property="Fill" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="Stretch" Value="Fill" />
    </Style>
    <Style x:Key="GridViewPinButtonStyle" TargetType="grid:GridViewPinButton">
        <Setter Property="PathStyle" Value="{StaticResource UnpinnedButtonStyle}" />
        <Setter Property="PathGeometry" Value="M4,2 L4,3 L8,3 L8,2 z M3,0 L4,0 L4,1 L8,1 L9,1 L9,2 L9,3 L9,6 L8,6 L4,6 L4,7 L3,7 L3,6 L3,4 L0,4 L0,3 L3,3 L3,2 L3,1 z" />
        <Setter Property="MinHeight" Value="22" />
        <Setter Property="MinWidth" Value="23" />
        <Setter Property="IsBackgroundVisible" Value="False" />
        <Setter Property="Command" Value="{x:Static telerik:RadGridViewCommands.TogglePinnedRowState}" />
        <Setter Property="CommandParameter" Value="{Binding}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Padding" Value="3" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="telerik:RadPathButton">
                    <Grid Background="Transparent">
                        <Border
                            x:Name="OuterBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}" />
                        <Path
                            x:Name="Path"
                            Margin="{TemplateBinding Padding}"
                            Data="{TemplateBinding PathGeometry}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                            Style="{TemplateBinding PathStyle}" />
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="BackgroundVisibility">
                                <VisualState x:Name="BackgroundIsHidden">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="OuterBorder" Storyboard.TargetProperty="Visibility">
                                            <DiscreteObjectKeyFrame KeyTime="0">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <Visibility>Collapsed</Visibility>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="BackgroundIsVisible" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType={x:Type grid:GridViewRow}}}" Value="True">
                            <Setter TargetName="Path" Property="Fill" Value="{telerik1:GreenResource ResourceKey=SelectedBrush}" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsPinned" Value="True">
                <Setter Property="PathGeometry" Value="M2,1L2,5L3,5L3,1zM3,0L6,0L6,1L6,5L7,5L7,6L4,6L4,9L3,9L3,6L2,6L1,6L0,6L0,5L1,5L1,1L1,1e-006L2,1e-006z" />
                <Setter Property="PathStyle" Value="{StaticResource PinnedButtonStyle}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style BasedOn="{StaticResource GridViewPinButtonStyle}" TargetType="grid:GridViewPinButton" />
    <ControlTemplate x:Key="RemoveItemButtonControlTemplate" TargetType="telerik:RadButton">
        <Grid x:Name="LayoutRoot" Background="Transparent">
            <Border
                x:Name="BackgroundVisual"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}" />
            <telerik:RadGlyph
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="9"
                Foreground="{TemplateBinding Foreground}"
                Glyph="{StaticResource GlyphClose}" />
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MouseOverBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=AccentHighBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
                <Setter Property="Opacity" Value="{telerik1:GreenResource ResourceKey=DisabledOpacity}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="SearchAutoCompleteBoxRemoveItemButtonStyle" TargetType="telerik:RadButton">
        <Setter Property="Template" Value="{StaticResource RemoveItemButtonControlTemplate}" />
        <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=StrongBrush}" />
        <Setter Property="Background" Value="{x:Null}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Margin" Value="2,0" />
        <Setter Property="MinHeight" Value="0" />
        <Setter Property="Width" Value="12" />
        <Setter Property="Height" Value="12" />
    </Style>
    <Style x:Key="SearchAutoCompleteBoxItemStyle" TargetType="mccb:SearchAutoCompleteBoxItem">
        <Setter Property="Foreground" Value="{telerik1:GreenResource ResourceKey=MarkerBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="2,0" />
        <Setter Property="Margin" Value="2,0,1,0" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="mccb:SearchAutoCompleteBoxItem">
                    <Grid>
                        <Border
                            x:Name="BorderVisual"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <ContentPresenter
                                    x:Name="contentPresenter"
                                    Margin="{TemplateBinding Padding}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                <telerik:RadButton
                                    x:Name="RemoveItemButton"
                                    Grid.Column="1"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Stretch"
                                    Command="mccb:SearchAutoCompleteBoxCommands.RemoveItem"
                                    CommandParameter="{Binding}"
                                    Cursor="Arrow"
                                    Focusable="False"
                                    IsTabStop="False"
                                    Style="{StaticResource SearchAutoCompleteBoxRemoveItemButtonStyle}" />
                            </Grid>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="contentPresenter" Property="Opacity" Value="{telerik1:GreenResource ResourceKey=DisabledOpacity}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <ControlTemplate x:Key="SearchAutoCompleteBoxTemplate" TargetType="mccb:SearchAutoCompleteBox">
        <Grid>
            <Border
                x:Name="BorderVisual"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="{TemplateBinding mat:MaterialAssist.CornerRadius}"
                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
            <ScrollViewer
                x:Name="PART_ScrollViewer"
                Height="{TemplateBinding Height}"
                Margin="{TemplateBinding BorderThickness}"
                Padding="0"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                Background="Transparent"
                BorderThickness="0"
                Focusable="False"
                HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                IsTabStop="False"
                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}">
                <mccb:SearchAutoCompleteBoxesItemsControl
                    x:Name="PART_Boxes"
                    Margin="{TemplateBinding Padding}"
                    Foreground="{TemplateBinding Foreground}"
                    IsTabStop="False"
                    ItemsSource="{TemplateBinding SelectedItems}"
                    SelectionBoxesVisibility="{TemplateBinding SelectionBoxesVisibility}">
                    <mccb:SearchAutoCompleteBoxesItemsControl.Editor>
                        <telerik:RadWatermarkTextBox
                            x:Name="WatermarkTextBox"
                            MinWidth="{TemplateBinding EditorMinWidth}"
                            Background="{x:Null}"
                            BorderThickness="0"
                            DisabledBackground="{x:Null}"
                            IsReadOnly="{TemplateBinding IsReadOnly}"
                            ReadOnlyBackground="{x:Null}"
                            WatermarkBehavior="HideOnTextEntered"
                            WatermarkContent="{TemplateBinding NullText}" />
                    </mccb:SearchAutoCompleteBoxesItemsControl.Editor>
                    <mccb:SearchAutoCompleteBoxesItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <mccb:SearchAutoCompleteBoxWrapPanel />
                        </ItemsPanelTemplate>
                    </mccb:SearchAutoCompleteBoxesItemsControl.ItemsPanel>
                </mccb:SearchAutoCompleteBoxesItemsControl>
            </ScrollViewer>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="ShowNullText" Value="False">
                <Setter TargetName="WatermarkTextBox" Property="WatermarkContent" Value="{x:Null}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="SearchAutoCompleteBoxStyle" TargetType="mccb:SearchAutoCompleteBox">
        <Setter Property="Template" Value="{StaticResource SearchAutoCompleteBoxTemplate}" />
        <Setter Property="FontFamily" Value="{telerik1:GreenResource ResourceKey=FontFamily}" />
        <Setter Property="FontSize" Value="{telerik1:GreenResource ResourceKey=FontSize}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Padding" Value="0,1" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
    </Style>
    <Style BasedOn="{StaticResource SearchAutoCompleteBoxItemStyle}" TargetType="mccb:SearchAutoCompleteBoxItem" />
    <Style BasedOn="{StaticResource SearchAutoCompleteBoxStyle}" TargetType="mccb:SearchAutoCompleteBox" />
    <Style x:Key="RadMultiColumnComboBoxStyle" TargetType="controls:RadMultiColumnComboBox">
        <Style.Setters>
            <Setter Property="mat:MaterialAssist.CornerRadius" Value="1" />
            <Setter Property="mat:MaterialAssist.FocusBrush" Value="{telerik1:GreenResource ResourceKey=AccentLowBrush}" />
            <Setter Property="Background" Value="{telerik1:GreenResource ResourceKey=MainBrush}" />
            <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=BasicBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontFamily" Value="{telerik1:GreenResource ResourceKey=FontFamily}" />
            <Setter Property="FontSize" Value="{telerik1:GreenResource ResourceKey=FontSize}" />
            <Setter Property="MinHeight" Value="22" />
            <Setter Property="DropDownMinWidth" Value="125" />
            <Setter Property="DropDownMinHeight" Value="100" />
            <Setter Property="DropDownHeight" Value="330" />
            <Setter Property="DropDownMaxHeight" Value="500" />
            <Setter Property="DropDownMaxWidth" Value="1000" />
            <Setter Property="EditorMinWidth" Value="50" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="controls:RadMultiColumnComboBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Border
                                x:Name="BackgroundVisual"
                                Grid.ColumnSpan="2"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding mat:MaterialAssist.CornerRadius}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            <mccb:SearchAutoCompleteBox
                                x:Name="PART_SearchAutoCompleteBox"
                                Grid.Column="0"
                                AutoCompleteProvider="{TemplateBinding AutoCompleteProvider}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                DeferTextChange="{TemplateBinding DeferTextChange}"
                                EditorMinWidth="{TemplateBinding EditorMinWidth}"
                                IsReadOnly="{TemplateBinding IsReadOnly}"
                                NullText="{TemplateBinding NullText}"
                                SelectedItems="{TemplateBinding SelectedItems}"
                                SelectionBoxStyle="{TemplateBinding SelectionBoxStyle}"
                                SelectionBoxStyleSelector="{TemplateBinding SelectionBoxStyleSelector}"
                                SelectionBoxTemplate="{TemplateBinding SelectionBoxTemplate}"
                                SelectionBoxTemplateSelector="{TemplateBinding SelectionBoxTemplateSelector}"
                                SelectionBoxesVisibility="{TemplateBinding SelectionBoxesVisibility}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            <telerik:RadDropDownButton
                                x:Name="PART_DropDownButton"
                                Grid.Column="1"
                                Padding="0"
                                BorderBrush="Transparent"
                                BorderThickness="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=BorderThickness, Converter={StaticResource ThicknessToOrientedThicknessConverter}, ConverterParameter=TopRightBottom}"
                                DropDownHeight="{TemplateBinding DropDownHeight}"
                                DropDownMaxHeight="{TemplateBinding DropDownMaxHeight}"
                                DropDownMaxWidth="{TemplateBinding DropDownMaxWidth}"
                                DropDownMinHeight="{TemplateBinding DropDownMinHeight}"
                                DropDownMinWidth="{TemplateBinding DropDownMinWidth}"
                                DropDownPlacement="{TemplateBinding DropDownPlacement}"
                                DropDownWidth="{TemplateBinding DropDownWidth}"
                                Focusable="False"
                                IsTabStop="False"
                                KeepOpen="{TemplateBinding KeepDropDownOpen}"
                                PopupPlacementTarget="{Binding ElementName=BackgroundVisual}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                                <telerik:RadDropDownButton.DropDownContent>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <ContentControl
                                            Grid.RowSpan="1"
                                            Grid.ColumnSpan="2"
                                            Content="{TemplateBinding ItemsSourceProvider}">
                                            <ContentControl.ContentTemplate>
                                                <DataTemplate>
                                                    <controls:RadGridView
                                                        AlternateRowStyle="{Binding AlternateRowStyle}"
                                                        AlternateRowStyleSelector="{Binding AlternateRowStyleSelector}"
                                                        AlternationCount="{Binding AlternationCount}"
                                                        AutoGenerateColumns="{Binding AutoGenerateColumns}"
                                                        BorderThickness="0"
                                                        CanUserGroupColumns="False"
                                                        CanUserSearch="False"
                                                        ColumnWidth="{Binding ColumnWidth}"
                                                        ContextMenu="{Binding ContextMenu}"
                                                        DataContext="{Binding}"
                                                        EnableLostFocusSelectedState="False"
                                                        GroupRenderMode="Flat"
                                                        IsReadOnly="{Binding IsReadOnly}"
                                                        IsSynchronizedWithCurrentItem="False"
                                                        ItemsSource="{Binding CollectionView}"
                                                        RowIndicatorVisibility="{Binding RowIndicatorVisibility}"
                                                        RowStyle="{Binding RowStyle}"
                                                        RowStyleSelector="{Binding RowStyleSelector}"
                                                        SearchStateManager="{Binding SearchStateManager}"
                                                        SelectionMode="Extended"
                                                        ShowColumnFooters="{Binding ShowColumnFooters}"
                                                        ShowColumnHeaders="{Binding ShowColumnHeaders}"
                                                        ShowGroupPanel="False"
                                                        ShowSearchPanel="False" />
                                                </DataTemplate>
                                            </ContentControl.ContentTemplate>
                                        </ContentControl>
                                        <Border
                                            Grid.Row="1"
                                            Grid.ColumnSpan="2"
                                            Background="{telerik1:GreenResource ResourceKey=PrimaryBrush}"
                                            BorderBrush="{telerik1:GreenResource ResourceKey=BasicBrush}"
                                            BorderThickness="0,1,0,0"
                                            Visibility="{TemplateBinding FooterVisibility}">
                                            <ContentPresenter
                                                x:Name="Footer"
                                                Margin="5"
                                                VerticalAlignment="Center"
                                                Content="{TemplateBinding FooterContent}"
                                                ContentTemplate="{TemplateBinding FooterContentTemplate}">
                                                <ContentPresenter.Resources>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                                        <Setter Property="Margin" Value="{Binding ElementName=CornerResizerIcon, Path=ActualWidth, Converter={StaticResource DoubleToThicknessConverter}, ConverterParameter=Right}" />
                                                    </Style>
                                                </ContentPresenter.Resources>
                                            </ContentPresenter>
                                        </Border>
                                        <Path
                                            x:Name="CornerResizerIcon"
                                            Grid.Row="1"
                                            Grid.ColumnSpan="2"
                                            Width="12"
                                            Height="12"
                                            Margin="2"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Bottom"
                                            Data="M8.0,0 L12,0 12,8 12,12 8.0,12 0,12 0,8 8.0,8 z"
                                            Fill="{telerik1:GreenResource ResourceKey=MouseOverBrush}"
                                            Stretch="Fill"
                                            StrokeThickness="0"
                                            Visibility="{TemplateBinding FooterVisibility}" />
                                        <Thumb
                                            x:Name="PART_HorizontalResizer"
                                            Grid.RowSpan="2"
                                            Grid.Column="1"
                                            Width="3"
                                            HorizontalAlignment="Right"
                                            Cursor="SizeWE"
                                            Opacity="0"
                                            Visibility="{Binding CanUserResizeDropDown, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        <Thumb
                                            x:Name="PART_CornerResizer"
                                            Grid.RowSpan="2"
                                            Grid.Column="1"
                                            Width="3"
                                            Height="3"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Bottom"
                                            Cursor="SizeNWSE"
                                            Opacity="0"
                                            Visibility="{Binding CanUserResizeDropDown, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        <Thumb
                                            x:Name="PART_VerticalResizer"
                                            Grid.RowSpan="2"
                                            Grid.Column="0"
                                            Height="3"
                                            VerticalAlignment="Bottom"
                                            Cursor="SizeNS"
                                            Opacity="0"
                                            Visibility="{Binding CanUserResizeDropDown, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    </Grid>
                                </telerik:RadDropDownButton.DropDownContent>
                            </telerik:RadDropDownButton>
                            <Border
                                x:Name="FocusVisual"
                                Grid.ColumnSpan="2"
                                BorderBrush="{TemplateBinding mat:MaterialAssist.FocusBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding mat:MaterialAssist.CornerRadius}"
                                IsHitTestVisible="False"
                                Visibility="Collapsed" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{telerik1:GreenResource ResourceKey=MouseOverBrush}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="FocusVisual" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter TargetName="PART_SearchAutoCompleteBox" Property="Background" Value="{telerik1:GreenResource ResourceKey=ReadOnlyBackgroundBrush}" />
                            </Trigger>
                            <Trigger Property="Validation.HasError" Value="True">
                                <Setter TargetName="FocusVisual" Property="Visibility" Value="Collapsed" />
                            </Trigger>
                            <Trigger Property="DropDownWidth" Value="NaN">
                                <Setter TargetName="PART_DropDownButton" Property="DropDownWidth" Value="{Binding RelativeSource={RelativeSource AncestorType=controls:RadMultiColumnComboBox}, Path=ActualWidth}" />
                            </Trigger>
                            <Trigger Property="FooterVisibility" Value="Visible">
                                <Setter TargetName="PART_CornerResizer" Property="Width" Value="{Binding ElementName=CornerResizerIcon, Path=Width}" />
                                <Setter TargetName="PART_CornerResizer" Property="Height" Value="{Binding ElementName=CornerResizerIcon, Path=Height}" />
                                <Setter TargetName="PART_CornerResizer" Property="Grid.Row" Value="1" />
                                <Setter TargetName="PART_VerticalResizer" Property="Grid.Row" Value="1" />
                            </Trigger>
                            <Trigger Property="CanUserResizeDropDown" Value="False">
                                <Setter TargetName="CornerResizerIcon" Property="Visibility" Value="Collapsed" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>
    <Style BasedOn="{StaticResource RadMultiColumnComboBoxStyle}" TargetType="controls:RadMultiColumnComboBox" />
</ResourceDictionary>
